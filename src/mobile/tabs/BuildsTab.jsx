import React from 'react';

export default function BuildsTab() {
  return (
    <section className="m-placeholder" data-testid="mobile-tab-builds">
      <div className="m-placeholder-title">Builds</div>
      <div className="m-placeholder-body">Coming in M5 — current build, log tail, abort.</div>
    </section>
  );
}
