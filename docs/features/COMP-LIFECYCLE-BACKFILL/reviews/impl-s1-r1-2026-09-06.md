# Implementation review — Slice S1, round 1 (2026-09-06)

Implementer: Codex `gpt-5.6-terra/high`, run `870e3206969f` (workspace-write, 555 s, 3.6M tokens).
Reviewer: Codex `gpt-5.6-terra/high`, run `c65530d8f566` (243 s). 3 findings (2 P1 + 1 P2), 3/3 CONFIRMED,
fixed by the controller; trivial fixes + one added test, so no round 2 (review-budget rule).

| # | Sev | Finding | Evidence | Fix |
|---|---|---|---|---|
| 1 | P1 | New `if (cmd === 'guard')` block at `bin/compose.js:1221` shadowed the canon-guard dispatcher at `:2106`; `compose guard status` printed the descriptors usage | Ran `node bin/compose.js guard status` → `Usage: compose guard descriptors` | Gate narrowed to `cmd === 'guard' && args[0] === 'descriptors'`; `guard status` verified working again |
| 2 | P1 | Printed sign command redirected the file on stdin with no filename, so `ssh-keygen -Y sign` could not write `<file>.sig` | `lib/guard-descriptors.js:115` | Command now ends in the quoted file path; verified by running the CLI |
| 3 | P2 | No S1 test touched the real stratum CLI: descriptor enumeration test had zero feature dirs, upgrade tests inject a fake client, wire tests mock `execFile` | `test/guard-descriptors.test.js:54` | New `test/stratum-client-guard-real.test.js`: real `guard policy` spawn on an unregistered id asserts stratum's canonical `guard_not_found` envelope and the R4-2 helpers on it |

## Controller checks beyond the review

- Cross-check of the S1-1 ↔ S1-5 seam: for all four modes, `deriveBackfillPolicy(legacyPolicyProjection(new))` is
  byte-equal (graph array order included) to the policy `buildPhaseGraph` now builds, and the legacy projection
  contains no `complete_backfilled`. This is the property the lazy upgrade's `from_checksum` match depends on.
- Live run of `compose guard descriptors` against this workspace: 0 descriptors, which is CORRECT — of the 35
  registered resources, the 3 real features (COMP-COMPLETION-GATE, COMP-COVERAGE-GATE, COMP-PLAN-IDEA-UNIFY) are
  terminal and skipped; the other 32 are test fixtures (FOO-1, TS-BUILD-*, BUG-*, FEAT-*) with no feature directory.
- Follow-up (not this feature): compose test suites register guards into the real `~/.stratum/guards` (32 fixture
  ids under this workspace's hash). They should run with `HOME` pointed at a temp dir, as the S2 golden flow will.
- Final targeted run (unsandboxed): 11 files, see progress.md for the count.
