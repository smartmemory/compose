# Brief: COMP-FABLE-ASTRA slice 3 — dispatch 3 (cross-seam golden + docs)

Repo: /Users/ruze/reg/my/forge/compose. Sibling stratum READ-ONLY. Do NOT git commit. Do NOT `npm install`.
Contract: blueprint-slice3.md §6 dispatch-3 row + Tests table "Integrated engine + connector" row + Documentation section; reports/slice3-d1-impl.md and slice3-d2-impl.md (what exists; their "Fixes r1" sections). NO production function edits in this dispatch — if a golden exposes a defect, STOP that golden, record the defect with a reproduction in your report, and continue with the rest; the fix goes back to the owning dispatch.

## Scope
1. NEW `test/integration/build-wave-golden.test.js` against the REAL stratum MCP server (pattern: `test/ts-cutover-consumer-fanout-golden.test.js:1255-1267` connects a real client; do not replace client.agentRun with the harness) and the fake codex executable from `test/helpers/fake-codex-project.js` (extend it — it is yours — to (a) record the `--model` argument it receives per invocation, (b) assert a prerequisite file's content exists before writing its own marker, (c) optionally write outside its files_owned on request, (d) optionally seed a cross-file wiring break whose unit test still passes). A TEST preset (yaml + sidecar in the test helper) with: `carry: { wave: { initial: ${plan.output.tasks}, on_revise: { assess_gate: ${assess.output.tasks} } } }`, plan/execute/execute_merge/verify/review/assess/assess_gate/ship, `verify: after: [execute_merge]`, execute `{default:'codex:implementer:critical', tier_from:'item.tier'}`, `_consumer.execute` ownership+independent+checkpoint_gate, assess_gate `decide_from` + WaveDecision validator, `_costCeiling`. Stub the claude steps (plan/verify/assess) with recorded outputs through the fake client path the d2 fixtures use; the codex fanout runs the real connector against the fake executable.
   Golden assertions (user-visible): two carried waves where wave 2 dispatches ONLY the tasks the assess decision names and unaffected wave-1 work is not re-run or re-merged; wave-2 worker sees wave-1's file (prerequisite assertion); the seeded wiring break is caught by `review` and repaired in the next wave; per-item `--model` received by the fake matches each task's tier; a task with unknown tier → zero fake-process invocations for that wave, flow ends failed; a worker edit outside files_owned → that task's step_done is the FILES_OWNED_VIOLATION failure, parent tree untouched; kill after ref publication and before journal acknowledgement, then resume → exactly one checkpoint, no re-applied diff; ship → one commit whose parent is the pinned base, no wave commits in ancestry; run record (stratum audit) shows the intended per-item model receipts.
   Timeout: this suite needs `--test-timeout=900000`; the real server needs `ps` and ports — if the sandbox blocks it, write the suite, run what you can, and say exactly which assertions are UNVERIFIED in sandbox for the controller to run on the host.
2. Docs: `README.md` (or `docs/pipelines.md` where sidecars are documented — pick the one that already documents `.profiles.json`): string vs object sidecar entries, `_` metadata (`_consumer`, `_costCeiling`), literal ownership paths, every named failure code (WAVE_*, FILES_OWNED_VIOLATION, OWNERSHIP_*, GATE_*, COST_CEILING_*, WAVE_CHECKPOINT_*), `--cost-ceiling-usd` and the pause/raise/resume flow, checkpoint branch `compose/wave/<flowId>` and `--fresh`. `docs/cli.md` for the CLI flag. `CHANGELOG.md` Unreleased entry for dispatch 3. Update `docs/features/COMP-FABLE-ASTRA/blueprint-slice3.md` with a short "Implemented" header block naming the real function names and test files (do not rewrite the body).
3. Report `docs/features/COMP-FABLE-ASTRA/reports/slice3-d3-impl.md` ≤ 80 lines: golden coverage vs the design's Completion evidence list (which items are now pinned by a test, with file:line; which remain live-fire only), defects found (with repro) and left for their owning dispatch, sandbox-unverified assertions, docs touched.

Gate: `node --test --test-timeout=900000 test/integration/build-wave-golden.test.js > /tmp/d3.log 2>&1; echo $?` plus the d2 targeted gate from briefs/slice3-d2-wiring.md. No full suite (controller runs it).
