# Brief: COMP-FABLE-ASTRA slice 3 — dispatch 1 (reusable primitives)

Repo: /Users/ruze/reg/my/forge/compose. Sibling stratum is READ-ONLY. Do NOT git commit. Do NOT `npm install`.
Contract: `docs/features/COMP-FABLE-ASTRA/blueprint-slice3.md` (sections 2-5, dispatch 1 row in §6, Tests table) as amended by the "Slice 3 blueprint adjudication" block in `docs/features/COMP-FABLE-ASTRA/progress.md`. Where the two differ, progress.md wins. Design background: `docs/features/COMP-FABLE-ASTRA/design.md` D2/D3/D4/D6.

## Scope of THIS dispatch (dispatch 1 of 3)
Build the primitives with their own tests; NOTHING in lib/build.js, lib/gsd.js, bin/compose.js or presets/ changes. Runtime behaviour stays opt-in/unreachable until dispatch 2 wires it. The full suite must stay green.

Files you own:
- NEW `lib/pipeline-profiles.js` — sidecar schema after slice 3: entries are `string | {default, tier_from?}` for agent steps, `{decide_from, validators?}` for gates, `_`-prefixed metadata (`_comment`, `_reduceSteps` preserved inert; `_consumer`, `_costCeiling` validated). Exports: `normalizePipelineProfiles(raw, spec)`, `mergeRuntimeProfiles(normalized, runtime)` (must not erase `tier_from` — blueprint C3 / build.js:2925 shallow-spread hazard), `resolveConsumerProfile(entry, item, provider)` (D6: tier from `item.tier` via the `tier_from` path, default when absent, unknown tier → typed error), `validateWaveAdmission(entry, items, opts)` (C13: validate EVERY item of the recorded input list — tier resolvable, `files_owned` present + literal repo-relative paths when ownership is configured, `depends_on` empty when `independent: true`; returns `{ok, findings[]}` and never dispatches), `profilesDigest(normalized)` (canonical JSON sha256). Slice 1's `preflightPipelineProfiles` in build.js (lines ~1249-1289 now) validates strings only; DO NOT edit build.js — instead export from this module a function the dispatch-2 wiring can call, and make sure `normalizePipelineProfiles` accepts every bundled sidecar under `presets/` unchanged (test it).
- NEW `lib/output-gate.js` — D2: `decideGateFromOutput({decide_from, validators}, stepOutputs, {gateStepId, gateToken, reviewOutput, ceiling})` → `{outcome: 'approve'|'revise'|'kill', rationale, source}` or `{outcome: null, reason}` when it must not auto-resolve (validator failure, unknown action value, overlapping value sets at config time, ceiling breached with `{breach:{spent, ceiling}}`). Named `WaveDecision` validators from design "Contracts": `open_count == len(open_findings)`, `blocking == review.output.blocking`, for `repair`: tasks non-empty and each task owns ≥1 file named by an open finding; `complete` ⇒ open_count 0 and not blocking; `blocked` ⇒ open_count>0. Pure function, no I/O.
- NEW `lib/wave-checkpoint.js` — D3 git plumbing, pure over a cwd: `prepareCheckpoint({cwd, ref, parentCommit, tree|workingTree, message})` writes a commit object via `commit-tree` WITHOUT touching HEAD/index/checked-out branch (use `GIT_INDEX_FILE` temp index or `write-tree` on a temp index — consumer-fanout.js:99-112 `withTemporaryIndex` already exists; reuse it, do not copy it), `publishCheckpoint({cwd, ref, expected, commit})` = compare-and-swap `update-ref` with the old value, `readCheckpointRef`, `worktreeBaseFor({journal, ref})` (returns the tip the next wave's worktrees start from), `squashOntoBase({cwd, ref, base})` for ship (produces the tree, does not commit — ship's existing staging/commit runs unchanged), `removeCheckpointRef` for `--fresh`. Recovery table from blueprint §5 implemented as `reconcileCheckpoint({journalEntry, refValue})` → one of the named actions.
- `lib/consumer-fanout.js` — (a) journal fields from blueprint §2 (`profilesDigest`, `waveAdmissions`, `issuance.itemBinding`, `issuance.resolvedProfile`, `issuance.ownership`, `issuance.findings`, `journal.wave.checkpoints[]` with `prepared|published` state), additive on the v1 journal, absent for legacy runs; `bindRunRevision` gains `profilesDigest` and `verifyConsumerRunRevision` refuses drift with `CONSUMER_PROFILE_REVISION_MISMATCH`. (b) D4: in the capture/merge path (`prepareIssuance` ~750 / `prepareMerge` ~861) compute the captured diff's changed paths from the RETAINED PATCH (`git apply --numstat`/`--summary` on the patch, or `diff --name-status` between baseline tree and captured tree — pick the one that handles renames/deletes/mode/binary and say why), compare against `issuance.itemBinding.item.files_owned` (literal paths; rename needs both endpoints owned; path escape `..`/absolute rejected), and on any path outside: mark the issuance failed with a named finding `{code:'OWNERSHIP_VIOLATION', taskId, files, allowedFiles, ...}` BEFORE it can be accepted for merge — a forged `files_changed` in the worker's result must not matter. (c) `#ensureWorktree` (~534) takes its base from `worktreeBaseFor` when a wave ref exists for the run, else HEAD as today. (d) recovery: `recoverAdvancedConsumerArtifacts` (~1390) recovers an approved checkpoint obligation even when the same gate is waiting again in a later round (C15). Keep every existing invariant: guarded `#mutate` + fsync-rename, no await inside a guarded mutation, `prepareMerge`/`applyMerge` transaction shape, cancel fences (`MergeAfterCancelError`), existing tests in `test/ts-cutover-consumer-fanout-golden.test.js` unchanged and green.
- NEW tests: `test/pipeline-profiles.test.js`, `test/output-gate.test.js`, `test/consumer-ownership.test.js`, `test/wave-checkpoint.test.js`, `test/helpers/consumer-wave-fixture.js` (disposable real git repo; reuse the fixture pattern at `test/ts-cutover-consumer-fanout-golden.test.js:521-562`). Assert user-visible outcomes (parent tree untouched after an ownership failure; HEAD/index/branch unchanged after a checkpoint; wave-2 base equals published checkpoint; every reconciliation row yields its named action; every bundled preset sidecar normalizes). No source-string assertions.

## Rules
- Real git in tests, no mocks of the code under test. `node --test --test-timeout=90000 <files> > /tmp/d1.log 2>&1; echo $?` — never pipe to tail.
- Then the full node suite once: `RESEND_API_KEY= STRIPE_API_KEY= npm test > /tmp/d1-full.log 2>&1; echo $?`. Sandbox cannot run `ps` or bind ports — name tests failing for that reason; the controller re-runs them on the host.
- CHANGELOG.md: one Unreleased entry for slice 3 dispatch 1 (primitives; not yet wired).
- Report: `docs/features/COMP-FABLE-ASTRA/reports/slice3-d1-impl.md` ≤ 80 lines: files, the D4 path-diff mechanism chosen and why, journal field versions, test counts, anything skipped and why, and the exact functions dispatch 2 must call (names + signatures).
