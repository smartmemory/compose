# Brief: COMP-FABLE-ASTRA slice 4 — contracts and the `team-fable-astra` preset

Repo: /Users/ruze/reg/my/forge/compose. Sibling stratum READ-ONLY (its validator is the oracle: `stratum validate` / the MCP `stratum_validate` tool, and `stratum/ts/src/ir/validate.ts` CARRY_* codes). Do NOT git commit. Do NOT `npm install`.
Contract: design.md ("Intent", "The loop", "Contracts", "Roles, models and budget", "Completion evidence"), the adjudication block in progress.md (Q1: `WaveDecision.tasks` is `Task[]`; Q2: assess_gate approve → ship, so a completed run ends in one base-parent commit), reports/slice3-d{1,2,3}-impl.md (the seams and the exact sidecar shapes they accept — dispatch 3's test preset in test/helpers is the closest working example; model on it, then on presets/team-feature.*).

## Deliverables
1. `presets/team-fable-astra.stratum.yaml` — steps `plan` (claude) → `execute` (codex fanout over `${wave}`, consumer, worktree, sequential, concurrency from input) → `execute_merge` (gate, on_revise: execute, max_rounds 2) → `verify` (claude, **`after: [execute_merge]`**, VerifyResult) → `review` (codex, fresh, read-only, ReviewFindings; input = goal, criteria, merged diff, VerifyResult — NEVER worker summaries) → `assess` (claude, WaveDecision) → `assess_gate` (gate: on_approve: ship, on_revise: execute, on_kill: null, max_rounds 2) → `ship`. Flow `max_rounds: 4`. `carry: { wave: { initial: ${plan.output.tasks}, on_revise: { assess_gate: ${assess.output.tasks} } } }` — carry refs are `${}`-only and illegal in when/set/ensure/iterate.until; every consumer of `wave` must be a dependency descendant of `plan` and inside assess_gate's reset closure; the revising gate must be `after`-ordered past every consumer. Inputs with the design's numeric defaults: `concurrency: 3`, `cost_ceiling_usd: 150`. Header comment in the style of team-feature (purpose/pattern/customize + the stratum carry rules). MUST pass `stratum validate` with zero errors — run it and paste the output in the report.
2. `presets/team-fable-astra.profiles.json` — per design "Roles" table: `plan`/`assess` = `claude:orchestrator:coordinator`; `execute` = `{ "default": "codex:implementer:critical", "tier_from": "item.tier" }`; `review` = `codex:read-only-reviewer:critical`; `verify` = `claude:orchestrator:standard`; `assess_gate` = `{ decide_from: { step: assess, field: action, approve: [complete], revise: [repair, implement], kill: [blocked] }, validators: [{ name: WaveDecision, review_step: review, tasks_field: tasks }] }`; `_consumer.execute` = `{ ownership: item.files_owned, independent: true, checkpoint_gate: execute_merge }`; `_costCeiling` = `{ input: cost_ceiling_usd, default: 150, gates: [assess_gate] }`. Must pass `preflightPipelineProfiles` (test it as the bundled-preset test in test/profile-preflight.test.js already does for the others — that loop picks it up automatically; make sure it passes).
3. Contracts in the yaml: `TaskGraph` with a NAMED `Task` (id, description, files_owned, files_read, depends_on, `tier: critical|standard|fast`, `tier_rationale`), ensures `len(result.tasks)` 1..6; `TaskResult` + `verification` (commands and outcomes); `VerifyResult`; NEW `ReviewFindings` (findings[]: severity, files, claim, evidence; `blocking`); NEW `WaveDecision` (action, tasks: Task[], rationale, addressed_findings[], open_findings[], blocking, open_count) with the design's scalar ensures (`complete` ⇒ open_count 0 and not blocking; `blocked` ⇒ open_count > 0). Check what the TS contract dialect actually supports for enums/nested named types (`stratum/ts/src/ir/schema.ts` + an existing preset) — if enums are not expressible, use string + ensure and say so.
4. Prompts with MUST checklists (feedback: vague step intents rubber-stamp): `plan` carries the tier guidance verbatim from design "Contracts" (critical for design judgment/root-causing, standard for brief-bounded implementation, fast for transcription-level edits; repair waves default to the repaired task's tier or higher, never lower), wave-1 must contain only mutually independent tasks (depends_on empty — later waves carry dependencies), no two tasks share files_owned, files_owned are literal repo-relative paths; `execute` item prompt = team-feature's plus "return verification {commands, outcomes}" and "touch only files_owned — anything else fails the task at merge"; `review` MUST: read the merged diff against goal + acceptance criteria + VerifyResult, probe cross-module wiring (a green unit suite is not evidence), never read worker summaries, one finding per defect with files + evidence; `assess` MUST: address every finding (addressed or open), set blocking from review, `repair` tasks own ≥ 1 file named by an open finding, `complete` only with zero open findings, `blocked` when repair cannot proceed.
5. `compose build <code> --team fable-astra` works through the existing `--team` rewrite (bin/compose.js ~2622-2627) — confirm named-preset resolution needs no change; add the preset to any list/help text that enumerates presets (grep `team-review`).
6. Tests: preset validates (real `stratum validate` via the installed CLI or the sibling's `node ts/dist/...`), sidecar preflights, a runBuild smoke through the d2 fixtures with the REAL preset yaml+sidecar and recorded claude outputs + fake codex: one wave → complete → ship = one commit (this is the preset's golden; put it in `test/build-team-fable-astra.test.js`). Docs: README/docs/pipelines.md preset list + a short section on the loop; `docs/cli.md` `--team fable-astra`; CHANGELOG Unreleased.
7. Report `docs/features/COMP-FABLE-ASTRA/reports/slice4-preset-impl.md` ≤ 80 lines incl. the `stratum validate` output, contract-dialect limits hit, and what live-fire (slice 6) still has to prove per the design's Completion evidence list.

Gate: `node --test --test-timeout=300000 test/profile-preflight.test.js test/build-team-fable-astra.test.js test/build-wave-routing.test.js test/build-output-gate.test.js > /tmp/s4.log 2>&1; echo $?` = 0. No full suite (controller runs it).
