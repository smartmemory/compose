# Brief: COMP-FABLE-ASTRA slice 3 — BLUEPRINT (no implementation)

Repo: /Users/ruze/reg/my/forge/compose. Sibling: /Users/ruze/reg/my/forge/stratum (read-only reference; `compose/node_modules/@smartmemory/stratum` is a symlink to `stratum/ts`).
Write EXACTLY ONE file: `docs/features/COMP-FABLE-ASTRA/blueprint-slice3.md`. Touch nothing else. Do not run the test suite. Another agent is editing lib/build.js (profile preflight near build.js:1227 and :2594) concurrently — read it, do not write it.

## What slice 3 is
`docs/features/COMP-FABLE-ASTRA/design.md` — read Revision note, "Existing foundation (verified)", "The loop, and what it depends on" (D2, D3, D4, D6), "Contracts", "Completion evidence", "Implementation slices" item 3. Slice 3 = four Compose seams, each usable by other presets, each with its own tests:
- **D2** output-driven gate resolution: sidecar-level `decide_from: { step, field, approve: [...], revise: [...], kill: [...] }` resolved by the build runner before falling through to policy / human. Plus the named validators the Contracts section lists for `WaveDecision` (run before gate resolution). Plus the cost-ceiling rule in "Roles, models and budget": ceiling exceeded ⇒ D2 does NOT auto-resolve; gate is left waiting for a human with the breach as the reason.
- **D3** wave checkpoints: after `execute_merge` approves, commit the integrated tree on `compose/wave/<flowId>`; next fanout's worktrees created from that tip; journal wave number + gate token BEFORE retained diffs are dropped; resume reconciliation rule (journal ahead of branch ⇒ re-apply retained diffs; branch ahead ⇒ trust branch if tip matches journal); at `ship` squash the branch onto base so ship's staging/commit is unchanged; failed/killed builds leave the branch, `--fresh` removes it.
- **D4** ownership at merge: before a task's captured diff is merged, compare its changed paths with `files_owned` of the engine-recorded descriptor `item` (Stratum STRAT-LOOP-CARRY shipped `item` on `ConsumerDispatchDescriptor`, computed as `values[item.index]` — see `stratum/docs/features/STRAT-LOOP-CARRY/blueprint.md` §2.6 and `stratum/ts/src/engine/engine.ts` `consumerDescriptor`). Neither the rendered prompt nor the worker's `files_changed` is trusted. Path outside ⇒ task fails with a named finding. Plan-time overlap check (`filesOwnedConflict`, build.js) stays.
- **D6** per-item tier: sidecar `"execute": { "default": "codex:implementer:critical", "tier_from": "item.tier" }`; tier resolved from descriptor `item` at dispatch, default when absent; unknown tier fails the wave BEFORE any dispatch; provider + template stay per stage; resolved model written to the run record per item. Note slice 1 (in flight) is adding `preflightPipelineProfiles` that validates every sidecar entry as a plain string — D6's object form must be accommodated by that preflight (spell out the change).

## What the blueprint must contain
Same discipline as `docs/features/COMP-BUILD-CANCEL/` (read its blueprint/plan for the format): every claim in the design about existing code is CHECKED against the real file:line and either confirmed or corrected in a numbered corrections table (Cn). Then per seam:
1. Exact insertion points (file:line, function name) in `lib/build.js`, `lib/consumer-fanout.js`, `lib/gsd.js`, `lib/step-prompt.js` or wherever, with the surrounding invariant you must preserve (the fanout journal's guarded writes, `prepareMerge`/`applyMerge` transaction, gate token binding, `resolveGateWithConsumerMerge`, the `review_gate` special case at build.js:~4551, policy fallthrough at ~4667, resume paths in `verifyConsumerRunRevision`/`recoverAdvancedConsumerArtifacts`).
2. The data shapes: where `decide_from`, `tier_from`, the wave journal, the per-item resolved model, and the D4 finding live; which existing stream events / run-record fields they extend. Sidecar schema after slice 3 (string | object entries; `_`-prefixed metadata).
3. The ordering of D2 relative to the existing review_gate branch and policy (decide_from must not break review_gate or plan_gate; say how the precedence reads).
4. D3: how `ConsumerFanoutArtifacts.#ensureWorktree` picks its base today (design says HEAD), what changes, how `applyMerge` lands in the parent tree, where the checkpoint commit goes, how `ship` (executeShipStep) squashes, how `--fresh` cleans, and the resume reconciliation table (journal vs branch states → action). Name the git plumbing you will use (commit-tree / update-ref vs checkout) and why it does not disturb the user's checked-out branch.
5. D4: where the captured diff's path list comes from (`cumulativeDiff`? the patch file? `git diff --name-only` on the retained diff), how renames/deletes are handled, glob vs literal semantics of `files_owned` (check what decompose prompts emit today), and what the "named finding" looks like on the task result and in the stream.
6. D6: exact dispatch site where `resolveAgentConfig(agentType)` runs for a consumer item (build.js ~3984 and the consumer issuance path in gsd.js `runConsumerIssuance`), how the descriptor `item` reaches it, and the fail-before-dispatch check for unknown tier for the WHOLE wave (all items validated before the first dispatch).
7. Tests per seam: name the test file, the real fixtures to reuse (`test/helpers/fake-codex-project.js`, fake stratum client, existing fanout goldens — find them), and the assertion that proves the user-visible outcome, not implementation detail. Flag any golden that needs the real stratum server or `ps` (sandbox cannot).
8. Implementation order and split into at most 3 dispatches with the file set each owns (no two dispatches editing the same function). Each dispatch must leave the full suite green.
9. Open questions for the owner ONLY where the design is silent and two readings lead to different code. Do not re-litigate owner decisions 1-5 in the Revision note.

Cap: 400 lines. Cite file:line for every existing-code claim. Where you could not verify, say UNVERIFIED rather than guess.
