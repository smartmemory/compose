# COMP-WORKSPACE-RESUME: Persist MCP workspace binding across restarts via CLAUDE_SESSION_ID env (when injected)

**Status:** PLANNED
**Created:** 2026-05-09

---

## Intent

Persist MCP workspace binding across restarts via CLAUDE_SESSION_ID env (when injected)

---

## Notes

_This is a seed design doc created by `compose feature`. The `compose build` pipeline will expand it into a full design, blueprint, and implementation plan._
