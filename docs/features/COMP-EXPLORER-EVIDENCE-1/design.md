# COMP-EXPLORER-EVIDENCE-1: The design phase prompt requires 2-3 explorer subagents but the accepted design ran one or none on both hosts (COMP-HOST-PORTABILITY-1 SD05) with no cardinality check or warning. Specify the required exploration evidence in the PhaseResult, validate it before accepting the phase, and make the subagent dispatch host-portable instead of Claude Agent/subagent_type semantics. Gap G10.

**Status:** COMPLETE (2026-09-17)
**Created:** 2026-09-16

---

## Intent

The design phase prompt requires 2-3 explorer subagents but the accepted design ran one or none on both hosts (COMP-HOST-PORTABILITY-1 SD05) with no cardinality check or warning. Specify the required exploration evidence in the PhaseResult, validate it before accepting the phase, and make the subagent dispatch host-portable instead of Claude Agent/subagent_type semantics. Gap G10.

---

## Notes

_This is a seed design doc created by `compose feature`. The `compose build` pipeline will expand it into a full design, blueprint, and implementation plan._
