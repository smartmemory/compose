# COMP-OUTCOME-ENUM-1: Agents return outcome success/done/approved/pass where the PhaseResult contract allows only complete|skipped|failed, forcing a full extra dispatch per occurrence (measured on host B and host C in COMP-HOST-PORTABILITY-1; 23 occurrences across stratum+compose via learn harvest). Decide: fix the step instructions/schema vocabulary, or normalize equivalent completion words before the costly retry. Check the steps first; do not loosen the contract blind. Gap G6.

**Status:** PLANNED
**Created:** 2026-09-16

---

## Intent

Agents return outcome success/done/approved/pass where the PhaseResult contract allows only complete|skipped|failed, forcing a full extra dispatch per occurrence (measured on host B and host C in COMP-HOST-PORTABILITY-1; 23 occurrences across stratum+compose via learn harvest). Decide: fix the step instructions/schema vocabulary, or normalize equivalent completion words before the costly retry. Check the steps first; do not loosen the contract blind. Gap G6.

---

## Notes

_This is a seed design doc created by `compose feature`. The `compose build` pipeline will expand it into a full design, blueprint, and implementation plan._
