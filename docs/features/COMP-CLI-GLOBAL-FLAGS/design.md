# COMP-CLI-GLOBAL-FLAGS: Pre-subcommand flag parser to enable compose --workspace=X build syntax

**Status:** PLANNED
**Created:** 2026-05-09

---

## Intent

Pre-subcommand flag parser to enable compose --workspace=X build syntax

---

## Notes

_This is a seed design doc created by `compose feature`. The `compose build` pipeline will expand it into a full design, blueprint, and implementation plan._
