# COMP-MODEL-ROUTE S1b: current-tree evidence

Evidence date: 2026-09-11. Compose checkout: `main`, HEAD `a341b58650e8d61478982c537b176a775b09f231`. S1a completion commit `2100b89` is present. Read-only sibling Stratum checkout: HEAD `813d799228c91e65ee8729d6a362bb17827a6747`. These identities were obtained with `git rev-parse` / `git log`; line references below refer to these working trees, not to the S1a commit. Initial Compose status contained the pre-existing untracked `docs/features/COMP-GUARD-CLAIM-1/audit.json`; no tracked changes were present. Sibling status was clean.

Method: targeted file reads, numbered source reads, file inventories and symbol/caller searches only. No tests, application launches, model calls, production imports or Stratum state writes were performed. **VERIFIED** means source inspected, not runtime-tested. **INFER** marks a consequence of the inspected code. Absence findings are bounded to the named modules/contracts and their searched callers. This is evidence, not an implementation proposal.

## Governing evidence

- **VERIFIED — scope:** S1b is attributable shadow outcomes; repair-floor enforcement belongs to S3. The explicit boundary overrides any reading of the later Decision 7 enforcement prose as an S1b deliverable (`docs/features/COMP-MODEL-ROUTE/design.md:311–324,416–428`). Missing attribution/cost is incomplete and excluded; confirmed cancellation is censored; successful flow termination alone does not establish acceptance (`design.md:239–264,281–307`, same directory).
- **VERIFIED — identity and continuation rulings:** the progress record separates physical run/epoch/item identity from logical work, retains cumulative completion evidence across GSD continuation, treats source epoch independently from consumer epoch, and explicitly defers receipt spooling to S1b (`docs/features/COMP-MODEL-ROUTE/progress.md:21–40,58–72,101–110`). Custom GSD ordinary-profile mismatch remains the recorded Q1 refusal, not authorization to alter its dispatch semantics (`progress.md:93–96`; `lib/gsd.js:1623–1632`).
- **VERIFIED — historical report caveat:** the D3 report retains an earlier “not all-green” status and failure account, but its later addendum records both production corrections. Current source contains the optional journal access and participating GSD recovery correction. Do not carry the earlier failures forward as still-open findings (`docs/features/COMP-MODEL-ROUTE/reports/slice1a-d3-impl.md:3,22–33,109–116`; `docs/features/COMP-MODEL-ROUTE/progress.md:201–210,223–234`; `lib/build.js:734`; `lib/gsd.js:1274–1319`). Historical test results in those documents were not rerun for this evidence file.

## Part A — cited-anchor verification

Every explicit code anchor in Decisions 5, 6 and 7 is included below. “Stale” means the cited numbers no longer identify the claimed code; it does not by itself mean the underlying claim is false.

| Decision / anchor as cited | Verdict | Current correct anchor and symbol; actual behavior |
|---|---|---|
| D5 `lib/build.js:1263–1268` | **STALE; behavior exists elsewhere.** | `runConsumerIssuance`, `lib/build.js:1779–1784`, calls `flowTag(flowId, descriptor.step ?? descriptor.id, descriptor.itemIndex)` for cancellation. `flowTag` conditionally constructs the tag at `lib/build-cancel.js:62–68`. The cited lines now compute logical wave identity and per-item admissions (`lib/build.js:1262–1268`). |
| D5 `lib/build.js:1352–1357` | **STALE; behavior exists elsewhere.** | `runConsumerIssuance`, `lib/build.js:1865–1875`, forwards usage with the primary dispatch id and parent `descriptor.step ?? descriptor.id`; it does not pass the item binding. The cited lines now describe source-output/carry provenance (`lib/build.js:1352–1358`). The nearby “Build mode passes no onUsage sink” comment is itself stale: Build installs `context.onUsage = context.recordBuildUsage` (`lib/build.js:4376`). |
| D5 `lib/build.js:1741–1765` | **STALE; substantive claim verified.** | `reportUsageReceipts`, `lib/build.js:2236–2298`, constructs the receipt at `2261–2285` without item binding, issuance token or `detail.routing`. The cited range now emits `build_step_start` / `item_model` and invokes the S1a issuance launch (`lib/build.js:1741–1765`). |
| D5 `../stratum/ts/src/engine/receipts.ts:4–14` | **ACCURATE.** | `ReceiptInput`, same range, has required `dispatchId` and optional `stepId`, `source`, `usage`, `telemetry`, `split`, `usdSource`, `detail`, `at`; no `flow`. `buildReceipt` validates/builds the receipt at `:26–60`. |
| D5 `lib/result-normalizer.js:570–599` | **ACCURATE branch location; starts one line into local invocation.** | `runAndNormalize`: primary branch chooses local Claude at `:563–589`, calling the SDK connector at `:569`; otherwise calls `stratum.agentRun` at `:591–599`. `dispatchId` is read after return at `:602`. This range is the primary MCP/local fork. |
| D5 `lib/result-normalizer.js:777–805` | **ACCURATE repair location; NOT another MCP/local fork.** | `runAndNormalize`'s `repairFn` always calls `stratum.agentRun` at `:777–782`, reads successful id at `:784–786`, constructs a usage record at `:788–795`, and reads error id at `:804–806`; error usage construction continues through `:815`. Even a local-SDK primary uses MCP for normalization repair. Any interpretation that both branches have local repair dispatch is contradicted by this code. |
| D5 `lib/consumer-fanout.js:710–721` | **STALE; claim true only for the Compose spool.** | `ConsumerFanoutArtifacts.recordPendingUsageReceipt`, `:850–863`, finds by dispatch id, compares payload digest, deduplicates identical payload and refuses changed payload. The cited lines now finish `bindRunRevision` and begin `#routingPut` (`:709–724`). Stratum's separate `usageReport` only checks duplicate dispatch id, not payload equality (`../stratum/ts/src/engine/engine.ts:890–892`). |
| D5 `lib/flow-state.js:57–73` | **ACCURATE interior anchor; broader strictness needs qualification.** | `readFlowSpend`, `:55–75`, rejects unacknowledged receipts, duplicate/missing ids, `costUnknown`, paid token/time receipts without USD, invalid USD and missing provenance. But absent USD with no positive tokens/ms is skipped at `:67–69`, and the function does not validate a routing join. It is not an existing implementation of the full S1b completeness rule. |
| D6 `lib/flow-state.js:40–51` | **ACCURATE description; return falls at line 52.** | `readFlowSnapshot`, `:41–52`, reads the one current `<flowId>.json`, validates identity/revision and optionally current waiting gate/token, and returns it. No historical snapshot reconstruction. |
| D6 `../stratum/ts/src/engine/state.ts:51–65` | **ACCURATE.** | `FanoutItemState`, same range, contains current index/status/generation, optional stage/epoch, live and accepted dispatch tokens, attempts, output and failure. It is a current-state interface, not an issuance-history ledger. |
| D6 `../stratum/ts/src/engine/state.ts:173–189` | **ACCURATE.** | `StepState`, `:173–194`, contains current status/epoch/attempts/spend/output/failure/live tokens and `acceptedDispatchToken`; its comment at `:176` documents omitted epoch as zero. Token presence and status are necessary local success evidence, not downstream acceptance. |
| D7 `presets/team-fable-astra.stratum.yaml:206–208` | **STALE.** | Assess prompt now requests repair tier equal to or above the repaired task at `:211–213`. The cited lines concern open-finding ownership, unaffected tasks and independent task lists (`:206–210`). This is prompt instruction, not enforced history-aware admission. |
| D7 `lib/output-gate.js:12–39` | **ACCURATE.** | `validateDecision`, `:6–42`, checks shape/actions/counts, review blocking equality, repair task ownership against open finding files and independent wave admission. No earlier executed-tier lookup/comparison. `decideGateFromOutput` validates current source/gate/review freshness at `:48–79`. |
| D7 `lib/pipeline-profiles.js:151–171` | **PARTLY STALE boundary; substantive no-floor claim accurate.** | `resolveConsumerProfile`, `:148–156`, resolves item tier or fallback. `validateWaveAdmission`, `:157–177`, checks profile resolution, independent dependencies and literal ownership collisions. The citation spans the tail of the former and part of the latter. Neither receives prior task/tier history. |

## Part B — S1b seam census

These tables distinguish actual model launches, usage handling, engine settlement and metadata. A routing issuance is not a model-call id: its identity includes the engine issuance token (`lib/model-router.js:97–102`), while the client independently mints call ids (`lib/stratum-mcp-client.js:268–297`; `lib/local-claude-connector.js:213`).

### B1. Per-call intent / completion / error observations

| Path and current symbols | VERIFIED behavior and relevant seam |
|---|---|
| Consumer execution shared by Build and GSD — `runConsumerIssuance` | Build calls it from its consumer launcher (`lib/build.js:4461`); GSD calls it from `runOneStep` (`lib/gsd.js:543–615`). It admits/pins the item, launches the routing issuance and calls `runAndNormalize` (`lib/build.js:1564–1568,1764–1797`). `isolation: none` enables the local path; this is not synonymous with every consumer using MCP. |
| Consumer model-less recovery paths | Artifact failure/recovery, previously accepted entries and prepared envelopes can report `stepDone` without another model call (`lib/build.js:1580–1645`). `reportConsumerStepDone` records result preparation and reconciles engine acceptance (`:723–794`). `prepareArtifactFailure` likewise has no model launch (`lib/consumer-fanout.js:1313`). These paths can complete an issuance without creating a new paid-call observation. |
| Consumer failure / timeout / cancellation | The catch distinguishes routing uncertainty, build cancellation, unconfirmed teardown, stuck observer, timeout and ordinary failure (`lib/build.js:1799–1861`). Failure usage reporting is separate from the eventual failure envelope; several branches catch usage-reporting errors and warn (`:1809–1839`). `buildCancel.cancelled` throws at `:1802` before that usage collection. Failure results can keep `usages` while setting `dispatchIds.repair` to null (`:1820–1858`). |
| Consumer success and local result settlement | Usage is emitted before final local validation/artifact preparation (`lib/build.js:1864–1905`); artifact envelope is persisted at `:1915–1921`, observed model metadata at `:1925–1927`, engine report at `:1948`, final local status at `:1982`. Local normalization/ownership failure and engine/downstream acceptance are separate observations. |
| Build ordinary primary | `runBuild` admits/prepares/launches at `lib/build.js:4762–4764`, calls `runAndNormalize` at `:4879–4900`, processes model failures/cancellation at `:4901–4953`, classifies local output at `:5209–5219`, reports usage at `:5224–5233`, then records result and `stepDone` / reconciliation at `:5237–5241`. |
| Build previous-failure fixer | A separate `runAndNormalize` call precedes the ordinary primary at `lib/build.js:4782–4806`; success and error usage are recorded separately at `:4808–4828`. It shares the current step context, but is another actual call, not another engine issuance automatically. |
| Build policy revision | Another `runAndNormalize` call occurs at `lib/build.js:4971–4991`. Rejected revision usage is still billed (`:4994–5002`); accepted revision receipts are reported immediately (`:5006–5012`) and aggregate result usage is subsequently combined (`:5015–5022`); failure usage/control errors follow at `:5029–5043`. Its normalizer may itself make a repair call. |
| Build review gate fixer | Dirty review has a separate `runAndNormalize` call stamped `review_fix` (`lib/build.js:5736–5751`); success/error receipts at `:5754–5773`, then revise at `:5777`. There is no new engine step issuance named `review_fix` in this call. Clean and dirty review special branches precede configured gate handling (`:5706–5787`). |
| GSD ordinary primary — easy to miss | `runOneStep` admits/prepares/launches routing (`lib/gsd.js:646–648`) but directly calls `stratum.agentRun` (`:651–660`), bypassing `runAndNormalize`. Error usage/id at `:661–670`; success usage/id at `:673–685`; JSON extraction/validation and result settlement at `:691–716`. This call does not pass a resolved profile or cancellation flow tag. Q1 refuses unsupported ordinary profile changes (`:1623–1632`). |
| GSD model-less steps | Ship interception uses `executeShipStep` / `stepDone`, and continuation decompose reuses recorded graph output without a model (`lib/gsd.js:619–641`). Build similarly intercepts ship before ordinary route admission (`lib/build.js:4752–4764`). |
| Normalizer primary MCP/local fork | `runAndNormalize` prepares execution options (`lib/result-normalizer.js:350–378`), subscribes to correlated events (`:401–496`), dispatches local Claude or MCP (`:563–604`), captures error usage/id (`:426–433,605–638`), and handles results arriving after timeout/interrupt/abort (`:646–663`). Success usage may come from event totals or returned usage (`:671–743`). There are no durable routing-call intent/completion/error hooks in these branches. |
| Normalizer repair, successful or failed, credited or uncredited | `repairFn` dispatches MCP only (`lib/result-normalizer.js:774–817`). Both returned and error ids/usage are captured; `normalizeReviewResult` makes one repair attempt and ordinarily swallows repair failure into tolerant fallback (`lib/review-normalize.js:141–173`). Cancellation/control exceptions are rethrown separately (`lib/result-normalizer.js:830–844`). Final `dispatchIds.repair` is conditional on `repairUsed`, although `usages` can include an uncredited repair (`:846–851`). Non-review/raw/fallback return paths are also at `:855–889`. |
| MCP actual-call wrapper | `StratumMcpClient.agentRun` funnels into `#dispatchAgentRun` (`lib/stratum-mcp-client.js:809–813,268–297`). It mints one id before invoke, attaches it non-enumerably to result/error, and emits best-effort dispatch telemetry. `invoke` can reject cancellation/options before RPC (`:302–332`); call id existence does not prove the provider executed. Cancellation id is separate (`:308`), and cancellation errors can carry usage/telemetry/split (`:353–355`). |
| Local SDK actual-call wrapper | `runLocalClaudeAgent(prompt, opts = {})` starts at `lib/local-claude-connector.js:126`; abort checks precede call id allocation (`:129,213`), actual SDK query starts at `:217`, observed events/usage at `:219–245`, failure usage at `:253–267`, success result at `:287–309`, late/abort/error preservation at `:313–336`. Result/error id attachment is non-enumerable (`:55–61`). |
| Direct text-call adapter | `StratumMcpClient.runAgentText`, `lib/stratum-mcp-client.js:831–908`, uses the same dispatch wrapper at `:896`, converts usage and calls an optional sink (`:855–889`), but returns only text (`:907`). Its sink catches and warns on errors (`:889`). Text return alone does not carry call identity to its caller. |
| Build gate Q&A — auxiliary observation | `makeAskAgent` calls `runAgentText('claude', …)` (`lib/build.js:2985–3030`, call `:3001`), forwards usage as `gate_qa` (`:3016–3024`), and is installed for the human gate (`:5854`). Repeated questions are distinct model calls without separate engine issuance tokens. |
| Bug escalation — auxiliary observations | `maybeRunEscalation` dispatches its tier-one/tier-two helpers (`lib/build.js:320–376`, invocation `:5335`). They call Codex / Claude `runAgentText` (`lib/bug-escalation.js:125,321`) with escalation usage callbacks (`:121,324`). These calls bypass the normalizer and do not create routing issuances themselves. |
| Codex startup probe — auxiliary, potentially paid | Build invokes the worktree probe at `lib/build.js:4172`; `preflightCodexWorktreeProbe` can skip by env/cache/non-git (`lib/codex-preflight.js:91–101`) or call `runAgentText('codex', …)` with preflight telemetry, no usage callback/flow issuance (`:138–148`). This precedes the main Build usage sink installation (`lib/build.js:4290–4376`). |
| Other consumers of the shared normalizer API | `lib/new.js:139,159`, `lib/import.js:217`, and `lib/step-validator.js:42` call `runAndNormalize` outside the Build/GSD routing driver. They are compatibility callers if the shared options/return/error contract changes; routing participation cannot be assumed from use of that function. |
| Engine-owned child calls | Stratum also settles engine fanout/judged usage internally (`../stratum/ts/src/engine/engine.ts:2399,2531–2580,2743–2767`). These are not Compose normalizer launches. The design explicitly leaves child observations unsupported and forbids counting them again in receipt totals (`docs/features/COMP-MODEL-ROUTE/design.md:262–264`). |

**VERIFIED — current S1a “launch intent” is issuance-level, not per call.** `prepareRoutingIssuance` and `launchRoutingIssuance` bind an engine descriptor/token (`lib/build.js:1092–1124,1169–1178`). `routingEvent` derives the event id from issuance id and event kind (`:1126–1134`), and the event state machine rejects repeated kinds (`lib/routing-ledger.js:466–479`). Thus the current launch event cannot represent primary + fixer + normalization repair as independently identified calls. This is distinct from the missing call-join surface, not evidence that existing launch persistence is absent.

**VERIFIED — participation boundary:** ordinary admission excludes fanout and non-agent steps (`lib/build.js:1063–1068`); supported consumer routing requires `dispatch: consumer` and exactly one stage (`:1301–1309`). Legacy profiled multi-stage consumers still run through the shared consumer code, with deferred rather than supported routing bindings (`lib/consumer-fanout.js:835–840`). Static start capture nevertheless inventories all declared stages (`lib/routing-ledger.js:206–228`). A pinned static stage therefore does not prove that it belongs to the supported outcome population. Scoped ordinary input also includes the retained subflow input (`lib/build.js:1075–1076`); flattening for routing snapshot reads occurs at `:831–837`.

### B2. Receipt intent, original binding, delivery and recovery

| Current seam | VERIFIED evidence |
|---|---|
| Original full item binding | `admitConsumerWave` obtains and validates the full recorded source/carry list, source accepted token/digest/epoch and consumer epoch, resolves every item/stage, and records each dispatch binding (`lib/build.js:1301–1419`, particularly `:1335–1375,1400–1415`). `recordDispatchBinding` validates item digest, profile pin and routing link and persists a clone keyed by engine dispatch token (`lib/consumer-fanout.js:817–845`). It is not keyed by paid-call dispatch id. |
| Ordinary binding | `admitOrdinaryRoute` binds input digest/provenance per current epoch; `prepareRoutingIssuance` binds current live token, item-null identity and static selection (`lib/build.js:1063–1124`). Ordinary observations have no consumer item object; the routing journal is nevertheless available for participating ordinary runs (`lib/build.js:4380–4401`). |
| Missing original join propagation | Consumer usage metadata forwards only parent step/source/primary id (`lib/build.js:1870–1875`). Build aggregates then calls `reportUsageReceipts` (`:4290–4316`); GSD's `recordTsAgentUsage` does the same (`lib/gsd.js:1565–1583`). Receipt construction lacks routing fields (`lib/build.js:2261–2285`). `flow.itemIndex` travels in model execution options, not in `ReceiptInput` (`lib/build.js:1782–1784`; `../stratum/ts/src/engine/receipts.ts:4–14`). |
| Existing durable receipt spool | `recordPendingUsageReceipt` / `acknowledgeUsageReceipt` store payload, pending/acknowledged state and optional engine sequence (`lib/consumer-fanout.js:850–872`). This is post-call receipt intent, not prelaunch model-call intent. `reportUsageReceipts` uses it only with `_costCeiling`; otherwise it sends directly (`lib/build.js:2287–2294`). Routing participation alone does not enable the spool. |
| Flush semantics and acknowledgement | `flushWaveReceipts` filters pending rows, refuses cancellation/no receipt surface, accepts recorded/already-recorded responses and optionally saves `seq` (`lib/build.js:803–819`). Acknowledgement does not itself validate a complete original routing join, and the spool does not compare its outer `dispatchId` against `receipt.dispatchId` (`lib/consumer-fanout.js:850–872`). |
| Zero-usage metadata shares delivery machinery | `reportWaveEvidence` creates `compose:<kind>:<flow>:<token>[:state]`, empty usage and detail, then spools/flushes (`lib/build.js:821–827`). Item-model proposed/observed, gate decisions, checkpoint and ship receipts use this path (`:1756–1761,1925–1927,1422–1447,1500,5821,5904–5906`). Ship replay flushes its existing receipt (`:6642–6653`). These ids are explicitly metadata ids, not paid-call attribution. |
| Receipt RPC and persisted row | `StratumMcpClient.usageReport` forwards the receipt unchanged (`lib/stratum-mcp-client.js:701–702`). Stratum `usageReport` checks the run and cancelled status, rejects reserved ids, deduplicates by id, validates/builds the row and locates the step (`../stratum/ts/src/engine/engine.ts:861–908`). The `ReceiptInput.detail` escape hatch exists (`../stratum/ts/src/engine/receipts.ts:4–14`). |
| Current-state enrichment overwrites top-level detail | `settleReceipt` canonicalizes the located parent step and adds current epoch/attempt/item information (`../stratum/ts/src/engine/engine.ts:2679–2722`); `receipt.detail = { ...receipt.detail, ...eventDetail }` at `:2738` lets current fields win. A nested `routing` object would survive this spread. The `usage_debit` event at `:2739` contains `eventDetail`, not the complete arbitrary receipt detail. Receipt rows and debit events are not interchangeable evidence sources. |
| Late lookup and reset | `locateReceiptStep` can consult retained current fanout/subflow state (`../stratum/ts/src/engine/engine.ts:3066–3085`), but `resetFrom` deletes that state (`:2823–2838`). An index-qualified receipt after replacement can encounter the new current item or no retained item. **INFER:** only a persisted original join can disambiguate that case; current enrichment cannot reconstruct the removed binding. |
| Build replay / resume | Routing replay uses `resumeRouting` / `reconcileRoutingIssuances` (`lib/build.js:981–1012,1135–1167,3998–4002`). Artifact recovery occurs in terminal probe and live resume (`:3923–3933,4008–4025`). Checkpoint/receipt recovery during context setup is gated on `waveProfilesEnabled`, not every routed ordinary step (`:4403–4410`); the predicate only checks wave-specific configuration (`:797–800`). |
| GSD continuation | Resume validates/settles the old routing run, seals epochs, creates continuation intent and exports routing ancestry (`lib/gsd.js:220–240`); new plan and new artifacts are bound at `:326–335`. `initializeRoutingJournal` copies routing records/token indexes/event tips (`lib/routing-ledger.js:325–340`), not the old artifact journal's top-level `dispatchBindings` or `pendingUsageReceipts` (`lib/consumer-fanout.js:825,852`). **INFER:** receipt evidence still located only in an old physical journal does not automatically accompany the new run. |
| Cancellation / terminal receipt policy | Compose's non-ceiling reporting skips cancelled runs (`lib/build.js:2237–2246`); Stratum rejects cancelled-run usage before duplicate-id detection (`../stratum/ts/src/engine/engine.ts:873–892`). Existing APIs therefore do not guarantee successful late receipt acknowledgement after cancellation. An unresolved local intent is not proof of free execution. |

### B3. Pre-reset wave, task and tier evidence

| Required evidence | Where it exists today, and what is not supplied |
|---|---|
| Full ordered item list and source | `admitConsumerWave` reads input, succeeded step output or carry and verifies against `state.fanout.items.length`; source and consumer epochs are distinct (`lib/build.js:1335–1375`). Current preset carries `plan.tasks` initially and `assess.tasks` on revise (`presets/team-fable-astra.stratum.yaml:90–94`). `recordWaveAdmission` persists legacy wave admissions; caller stores item index/digest/ownership/profile projections (`lib/consumer-fanout.js:794`; `lib/build.js:1406–1409`). Full item objects reside in dispatch bindings (`:1413–1415`; `lib/consumer-fanout.js:839`). |
| Task ids / logical lineage | `admitRoutingWave` creates logical wave/epoch identities, logical task ids and all item admissions, rejecting duplicate logical ids (`lib/build.js:1238–1299`, identity `:1262–1275`, duplicates `:1287`). For an item without `id`, it derives a logical task id from wave/index; this is an allocation identity, not a claim that an earlier task was retained. The preset Task schema has id, files, dependencies and tier, but no prior-issuance reference (`presets/team-fable-astra.stratum.yaml:23–30`). |
| Item digest / index / generation / stage / epoch | Admission records include physical and logical identity and input digest/provenance (`contracts/routing-record.schema.json:520–544`). Issuance records add live engine token, prior record, static selected/would routes (`:668–689`). Live state gives current item generation/stage/status/tokens (`../stratum/ts/src/engine/state.ts:51–65`). `prepareRoutingIssuance` checks the current snapshot before making this immutable identity (`lib/build.js:1092–1124`). |
| Chosen tier and pinned provider mapping | Start contains original/merged profiles, mapping and static resolutions (`contracts/routing-start.schema.json:179–245`); mapping capture includes provider candidate/coordinator tiers (`lib/routing-ledger.js:248–258`); item/profile selection occurs at admission (`lib/build.js:1265–1285,1380–1400`). This is intended/static execution configuration, not observed execution. |
| Actually executed model/effort/tier | Model/effort may appear on primary usage from MCP/local connector (`lib/result-normalizer.js:288–321,671–743`). Local success telemetry returns model/duration without effort (`lib/local-claude-connector.js:287–300`), while its separate dispatch telemetry records configured `appliedEffort` (`:68–90,151`). Model can begin as configured model before an observed init event (`:197,219`). Current `item_model` observed metadata explicitly says `connectorIdentityVerified: false` (`lib/build.js:1925–1927`). **NOT FOUND:** a joined, validated executed-tier derivation in current routing records; S1a selection checks static equality (`lib/routing-ledger.js:40–45`). |
| Issuance and accepted tokens/statuses | Current engine tokens/statuses are available before reset (`../stratum/ts/src/engine/state.ts:51–65,173–194`). Final consumer success writes accepted token; intermediate stages clear it while advancing stage (`../stratum/ts/src/engine/engine.ts:2063–2079`). Ordinary success writes acceptance (`:855`); failure clears or rotates tokens (`:2009–2010,2051–2056,2478–2503`). S1a reconciles only matching accepted token / epoch / generation (`lib/build.js:1135–1167`). |
| Existing retained epoch evidence | `sealRoutingEpochs` binds admission ids, settled issuance ids, source binding, graph and graph digest (`lib/build.js:1188–1237`). It is consumer-only, requires every admitted allocation to have settled issuance evidence and freezes an already-sealed issuance set (`:1205–1214`). New wave admission invokes it (`:1241`); consumer completion can invoke it after all succeeded (`:740–750`); GSD continuation invokes it before replacement (`lib/gsd.js:220–240`). This is not a gate-time full status/tier/disposition snapshot. |
| Existing continuation graph proof | `createContinuationIntent` checks settled old issuances, exact source graph, cumulative verified completion, filtered graph/dependency/index transformation (`lib/routing-ledger.js:492–574`). Its schema preserves source/filtered graphs, completion chain and bindings (`contracts/routing-record.schema.json:161–178`). This records GSD graph transformation, not every revise-gate acceptance decision. |
| Artifact acceptance and merge history | `#reconcileAuditInto` marks entries accepted only on current succeeded/matching accepted token, and supersedes entries missing after generation/index changes (`lib/consumer-fanout.js:1068–1100`). `prepareIssuance` stores worktree or isolation-none envelopes (`:1198–1280`). `prepareMerge` gathers accepted entries and transaction evidence (`:1352–1485`), but checkpoint accepted tokens are worktree-only (`:1375,1470–1485`), and blocked transactions differ (`:1401–1413`). These are artifact/checkpoint meanings of “accepted,” not the downstream label required by D6. |
| Exact reset boundary | Compose sends actual gate resolution from Build's resolver (`lib/build.js:5586`) or GSD (`lib/gsd.js:764`). Stratum's `gateResolveLocked` validates live gate token (`../stratum/ts/src/engine/engine.ts:1298–1317`), evaluates carry before reset (`:1345`), clears gate token and records `gate_resolved` (`:1349–1350`), updates carry (`:1363–1367`), then calls `resetFrom` (`:1370`). The gate RPC can therefore have destroyed the pre-reset state before returning to Compose. |
| What reset preserves vs destroys | `resetFrom` computes a forward dependency closure (`../stratum/ts/src/engine/engine.ts:2784–2802`), records a minimal reset event/engine receipt with step epoch transitions and dropped subflows (`:2805–2818`), then clears attempts/spend/output/failure/routing/live and accepted tokens (`:2823–2831`), increments epoch/fanout epoch and deletes fanout/subflow (`:2835–2838`). The event is not a copy of old items, outputs or accepted tokens. Current receipt spine survives; current task structures do not. |
| Other destructive projection | `cleanupWorktrees` discards diffs but keeps binding evidence (`lib/consumer-fanout.js:1773–1799`); Build calls cleanup on engine terminal response (`lib/build.js:5961–5963`) and artifact recovery may call it on already-advanced/terminal gates (`lib/consumer-fanout.js:1957–1958`). No claim of complete old outcome history can rest on a later worktree scan. |

### B4. Gate evidence and acknowledged-transition recovery

| Path | VERIFIED capture / decision / recovery behavior |
|---|---|
| Output-driven gate validation | `decideGateFromOutput` validates gate config, succeeded decision-source output, gate/source epoch freshness, action mapping and configured review freshness; returns cloned output plus source accepted token/digest (`lib/output-gate.js:48–79`). `validateDecision` handles task/finding shape and ownership (`:6–42`). The returned evidence is current decision evidence, not a persisted old-wave task snapshot. |
| Cost-ceiling and configured gate wrapper | `evaluateConfiguredGate` flushes receipts and reads strict spend, can emit `gate_hold`, builds the decision context, emits proposed evidence and re-audits freshness (`lib/build.js:1451–1508`, especially `:1462–1475,1490–1505`). This path is conditional on output/ceiling configuration. It is not reached at every gate. |
| Build actual common resolver | Waiting-gate audit/token at `lib/build.js:5415–5423`; merge gate identification/artifacts at `:5452–5483`; resolver closure starts `:5486`. Merge failure or checkpoint errors can turn a requested approve into revise/kill (`:5504–5561`). Actual gate RPC occurs at `:5586`; post-RPC mark/cleanup at `:5633`. Proposed action and acknowledged action can differ. |
| Build gate decision branches | Review clean auto-approve (`lib/build.js:5706`); dirty-review fixer and revise (`:5736–5777`); configured output gate / hold (`:5787–5796`); policy skip (`:5811`, accepted metadata `:5821`); flag policy (`:5829`); human gate + Q&A (`:5854`) and human proposal/resolve/accepted evidence (`:5904–5906`). A configured hold returns while gate is waiting; it is not a terminal acceptance label. |
| Build acknowledgement-loss catch | After `gateResolve` failure, recovery audits and uses the number/ordinal of `gate_resolved` events to identify the transition, only under `waveProfilesEnabled` (`lib/build.js:5598–5627`, particularly `:5601–5602`). Ordinary routing-only gates do not acquire this behavior just because routing is on. |
| GSD gates | `runOneStep` audits waiting gate (`lib/gsd.js:720`), evaluates config / returns hold (`:730–733`), constructs merge-aware resolution (`:734–763`), sends gate RPC (`:764`), recovers by event ordinal (`:767–768`), then marks/publishes/cleans artifacts and emits accepted evidence (`:777–784`). This is a second driver-owned gate boundary in addition to Build. |
| Existing durable artifact gate evidence | `prepareMerge` persists gate-token transaction, ordering/checkpoint information and, in the wave branch, gate ordinal (`lib/consumer-fanout.js:1352–1485`). `markGateResolved` stores mutable outcome/time (`:1749`), `markGateAdvanced` records advancement (`:1763`). Neither is a full findings/dispositions + old task/tier snapshot. |
| Recovery after process restart | `recoverAdvancedConsumerArtifacts` reads existing transactions and audit events, chooses event by gate step and ordinal, restores unacknowledged merge baseline or recovers approved checkpoint, then cleans up (`lib/consumer-fanout.js:1916–1960`). Build calls this in terminal probe and resume (`lib/build.js:3926,4017`). It does not match `gate_resolved` by token because that event does not contain the gate token (`../stratum/ts/src/engine/engine.ts:1338,1350`). |
| Revise ceiling / kill special case | Stratum's rounds-exhausted revise branch clears gate token, emits gate resolution and terminalizes without the normal reset path (`../stratum/ts/src/engine/engine.ts:1337–1341`). Normal kill and normal revise/approve branch after gate event (`:1349–1370`). A snapshot tied only to successful revise-reset would omit this outcome. |
| Lineage evidence currently absent | WaveDecision has action/tasks/rationale/addressed/open findings/blocking/count, but no explicit previous task/issuance lineage refs (`presets/team-fable-astra.stratum.yaml:54–61`). Prompt requests exact finding partition and retention of unaffected tasks (`:203–207`), whereas validation does not establish that partition against the original review finding set (`lib/output-gate.js:12–39`). Ambiguous identity/ownership therefore remains ambiguous evidence under D6 (`docs/features/COMP-MODEL-ROUTE/design.md:276–278`). |

### B5. Terminal/reconciled append and latest validated version

**VERIFIED — no existing outcome ledger implementation:** `lib/routing-ledger.js` implements immutable start/plan/run/continuation documents and journal validation; its header explicitly excludes an outcome ledger (`:1–3`), schema loading covers only start/record (`:9–18`), immutable record persistence is at `:115–156`, and the last exports are pending-plan/read-binding operations (`:576–592`). A targeted search for `ledger.jsonl`, `routing-join`, and `routing-outcome` in `lib` and `contracts` found no routing implementation. The requirement exists in `docs/features/COMP-MODEL-ROUTE/design.md:305–307,479–481`.

**VERIFIED — required labels, not existing classifications:** D6 names `accepted`, `repaired`, `re-implemented`, `retried-same-epoch`, `failed-or-cancelled`, and `unknown`; confirmed cancellation is censored even when replacement later occurs (`docs/features/COMP-MODEL-ROUTE/design.md:281–303`). Existing routing events have only issuance launch/result/settlement/uncertainty (`contracts/routing-record.schema.json:790–1059`), and artifact state acceptance uses current engine tokens (`lib/consumer-fanout.js:1094–1099`). Neither currently derives those six downstream labels or their one-latest-version population.

| Existing storage/lifecycle boundary | VERIFIED evidence relevant to terminal or reconciled observations |
|---|---|
| Project storage and durability | `ensureRoutingStorage` checks real paths/symlinks/tracked routing subtree and adds local git exclude (`lib/routing-ledger.js:83–96`). `immutable` writes canonical bytes + newline through exclusive temp, fsync, hardlink/no-clobber and directory sync (`:115–130`). `withStartLock` serializes by start id using a directory/PID lock (`:132–150`). This is not a project-wide JSONL append lock or latest-version reader. |
| Consumer journal durability | `#mutate` reloads, validates, preserves byte identity on no-op and atomically rewrites the whole JSON journal (`lib/consumer-fanout.js:410–436`; durable write `:70–82`). Its writer guard is a module-local set (`:85–89`), not cross-process/project coordination. Routing records are immutable by id (`:717–724`); a changed outcome revision cannot currently replace one of those records under the same id. |
| Engine result reconciliation | `reportConsumerStepDone`, `reportRoutingStep`, `reconcileRoutingIssuances` are successful response/replay/acknowledgement-loss boundaries (`lib/build.js:723–794,1180–1184,1135–1167`). Epoch sealing follows settled admissions (`:1188–1237`). No function here derives the D6 final labels or appends a revisioned learning row. |
| Gate reconciliation | Both driver gate closures, acknowledgement-loss catches and restart artifact recovery can learn an outcome after the original call has returned/thrown (`lib/build.js:5486–5633`; `lib/gsd.js:734–784`; `lib/consumer-fanout.js:1916–1960`). Terminal append is not the only point at which outcome evidence becomes available. |
| Build normal terminal | Consumer queue/drain boundaries precede final handling (`lib/build.js:4541–4598,5957–5959`); engine terminal cleanup at `:5961–5963`; cancelled/completed/failed/budget/killed handling at `:5965–6030`. Flow completed can still be followed by a failing completion/health check (`:5984–5997,6128`); application terminal status and per-issuance downstream acceptance are different facts. |
| Build thrown/cancelled/teardown/restart terminal | Cancellation teardown has separate ownership (`lib/build.js:4128–4130`; `lib/build-cancel.js:168`); outer catch stands down when teardown owns completion and otherwise terminalizes thrown Build (`lib/build.js:6352–6382`). Finalization may be called from both finally layers (`:6383–6420`). Already-terminal resume probe has its own early path (`:3919–3952`). These paths do not all pass through normal completed handling. |
| GSD terminal / pause / exception | Main loop and waiting gate return (`lib/gsd.js:348–360`); stuck/budget pause handling (`:363–394`); completion handling (`:405–425`); normalized terminal status / state write (`:431–455`); thrown path after planning checkpoint (`:460–473`). Resume/continuation has old and new physical runs (`:220–240,326–335`). A pause is not proof of permanent failure or acceptance. |
| Late receipt update | Pending delivery/acknowledgement remains in `flushWaveReceipts` and journal methods (`lib/build.js:803–819`; `lib/consumer-fanout.js:850–872`); no current operation attaches that acknowledgement as a new validated outcome version. Engine duplicate receipt response is not a ledger revision (`../stratum/ts/src/engine/engine.ts:890–892`). |
| Other existing “ledger” is not this one | `lib/dispatch-ledger.js:14` uses a different data path; allowed event fields (`:54–72`) lack the full routing join; append (`:268–275`) is dispatch telemetry with its own counters, not immutable logical outcome id/revision semantics. `settleDispatches` uses it for local normalization/policy settlement, skips GSD and distinguishes credited repair (`lib/build.js:2768–2820`). It does not establish downstream acceptance. |

## Part C — S1a surfaces already present

### C1. All exports of `lib/model-router.js`

The export inventory was verified by source and `rg '^export '`: **11 exports**.

| Export / signature | VERIFIED current responsibility |
|---|---|
| `RoutingError`; `constructor(code, message)` | Error with routing code/name (`lib/model-router.js:4–6`). |
| `routingRefuse(code, message)` | Throws `RoutingError` (`:7`). |
| `canonicalRoutingJson(value)` | Strict canonical finite JSON; sorted object keys, order-preserving arrays; rejects unsupported values, cycles/prototypes/symbols/decorated or sparse arrays instead of silently serializing (`:12–30`). |
| `routingDigest(value)` | SHA-256 over canonical UTF-8 JSON (`:32`). |
| `reachableContracts(root, definitions = {})` | Retained reachable grammar closure from traversal (`:83`, traversal `:35–82`). |
| `contractFingerprint({ root, contracts = {}, options = [], paths = [] }, version = 1)` | Version-one fingerprint over reachable contracts and canonical sorted option/path sets (`:84–89`). |
| `dispatchKey({ preset, scopedStep, stage, provider, template = '', prior, fingerprint })` | Statistical canonical key, including stage/provider/template/prior/contract; not a physical run key (`:91–95`). |
| `routingRecordId({ runId, scopedStep, stage, epoch, itemIndex, generation, issuanceToken })` | Digest of physical issuance identity, validates missing/invalid members; ordinary item index/generation are paired nulls (`:97–102`). |
| `routingTable()` | Frozen empty `s1a-empty-v1` table description/digest, null cutoff (`:103`). |
| `assertRoutingSlice(start)` | Supports off/shadow only; refuses trials/exploration/calibration feedback outside S1a (`:104–108`). |
| `resolveRoute({ start, key, baseline, allocationId, manualOverride })` | Static baseline proposal/admitted/would with static source/provenance; refuses unsupported inputs. `key` and `allocationId` are accepted but not used for learning/selection (`:110–119`). |

### C2. All exports of `lib/routing-ledger.js`

The export inventory was verified by source and `rg '^export '`: **19 exports**. All are synchronous functions in the current module.

| Export / signature | VERIFIED current responsibility |
|---|---|
| `routingStaticInput(input)` | Removes routing transport fields listed at `lib/routing-ledger.js:19`, preserving static input (`:27`). |
| `validateRoutingStart(start)` | AJV/strict canonical validation and root/table/input/spec/seed/contract digest checks (`:28–35`). |
| `validateRoutingRecord(record, pin)` | Closed record schema/root pin; issuance id/static selection, admission invariants, plan/epoch/continuation digest and graph checks (`:37–68`). |
| `ensureRoutingStorage({ cwd })` | Validates/creates local routing storage and git-local exclude; refuses tracked routing storage (`:83–96`). |
| `validateRoutingTransport(spec)` | Refuses unsafe interpolation/forwarding of reserved transport inputs, including whole input references (`:163–185`). |
| `createRoutingStart({ cwd, spec, inputs, originalProfiles = {}, runtimeOverrides = {}, runtimeOrigins: suppliedOrigins = {}, preflight, mode = 'off', presetId, hooks = {} })` | Returns null in off mode; creates immutable root, seed, pinned spec/input/mappings/static resolutions and policy in shadow (`:187–245`). |
| `routingModelMappings(preflight)` | Copies preflight resolutions and provider tier/coordinator mappings used for current static routing (`:248–258`). |
| `readRoutingStart({ cwd, startId, rootDigest })` | Reads, validates and verifies workspace/root pin (`:260–263`). |
| `recordRoutingPlanIntent({ cwd, start, input, specDigest, featureCode, previousRunId = null, continuation = null, hooks = {} })` | Persists prepared plan intent, optional continuation document and feature index (`:265–280`). |
| `recordRoutingPlanRequested({ cwd, start, intent, hooks = {} })` | Persists separate requested-phase record before plan RPC (`:282–285`). |
| `bindRoutingRun({ cwd, start, intent, snapshot, hooks = {} })` | Validates request and engine snapshot, persists root-bound physical run binding and unique plan→run pointer (`:287–300`). |
| `recoverRoutingPlan({ cwd, intent, stateRoot })` | Finds exactly one matching persisted run or refuses uncertain plan recovery (`:302–308`). |
| `validateRoutingRun({ cwd, snapshot, journal, currentSpec, currentMappings })` | Checks current/pinned spec/input/mapping/workspace/revision and routing journal; returns `{ start, binding }` (`:310–323`). |
| `initializeRoutingJournal(binding, retained = null)` | Creates closed routing subtree; continuation retains validated predecessor records/token index/event tips and adds current binding (`:325–340`). |
| `validateRoutingJournal(routing, binding)` | Closed subtree, record schemas, root/ancestry, admission and issuance references, logical identity, continuation chains, epoch graph references, event completeness (`:369–455`). |
| `routingIssuanceState(routing, issuanceId)` | Replays contiguous unique event sequence with event-tip validation and token-bound settlement; returns `{ state, envelope }` (`:457–488`). |
| `createContinuationIntent({ start, oldBinding, resumeDetails, priorJournal, snapshot })` | Validates prior settled routing and current graph, cumulative completed tasks/results, filtering/dependency removal/index mappings and bindings; returns continuation record, without itself writing it (`:492–574`). |
| `pendingRoutingPlans({ cwd, featureCode })` | Reads feature's indexed starts/intents/request/bindings, returns `{ start, intent, requested, bound, binding }`; despite its name, includes bound entries (`:576–588`). |
| `readRoutingRunBinding({ cwd, start, runId })` | Reads expected root-bound run record by derived identity (`:590–592`). |

### C3. Consumer journal routing fields and associated methods

| Existing structure / operation | VERIFIED meaning |
|---|---|
| Constructor and journal initialization | `ConsumerFanoutArtifacts({ runId, targetCwd, artifactRoot, hooks = {}, revisionDigest, specDigest, profilesDigest, routingBinding, routingAncestry })` validates supplied routing binding and initializes routing only for participation (`lib/consumer-fanout.js:355–382,533–559`). Default journal location is under the temporary consumer-artifact root, keyed by target/run (`:282–296`), not `.compose/routing/ledger.jsonl`. |
| `journal.routing` | Exactly `version`, `startId`, `rootDigest`, `runBinding`, `records`, `tokenIndex`, `eventTips` (`lib/routing-ledger.js:338–340,372–374`). Records include current run binding and retained continuation ancestry; tokenIndex maps engine token→issuance id; eventTips binds issuance→`{ count, eventId }` (`:325–340,457–465`). No joins/outcomes/ledger cursor fields exist. |
| Reload validation | Journal reload verifies routing pins and top-level dispatch bindings against routed/deferred identities (`lib/consumer-fanout.js:467–530`, especially `:485–487`). Adding arbitrary top-level fields to the routing subtree is not accepted by its closed-field validator (`lib/routing-ledger.js:372–374`). |
| `recordRoutingRecord(record)` / `recordRoutingAdmissions(records)` | Immutable insert; issuance/event are routed to specialized methods; admission batch commits atomically (`lib/consumer-fanout.js:727–739`). |
| `exportRoutingJournal()` / `readRoutingRecord(id)` | Reload and return cloned routing subtree/required record (`lib/consumer-fanout.js:741–751`). Export is not the full consumer journal. |
| `recordRoutingIssuance({ issuance })` | Atomic immutable issuance + token index + initial event tip; same-admission retry must link settled predecessor, prevents forks (`lib/consumer-fanout.js:753–775`). |
| `recordRoutingEvent(event)` | Validated immutable insertion and contiguous tip extension, with identical replay no-op (`lib/consumer-fanout.js:777–791`). |
| `dispatchBindings[dispatchToken].routing` | Exact `{ rootDigest, recordId, admissionId, logicalTaskId, logicalWaveId, logicalEpoch }`; selected resolution must match dispatch profile (`lib/consumer-fanout.js:827–845`). Sibling fields are cloned `itemBinding` and `resolvedProfile`. |
| Deferred dispatch binding | A participating unsupported dispatch stores validated `deferred: { flow, step, stage }` instead of a routing issuance link; omission in a participating journal is not silently adopted (`lib/consumer-fanout.js:835–840,467–530`). |
| Non-routing top-level evidence | `waveAdmissions`, `dispatchBindings`, `pendingUsageReceipts`, wave/checkpoint and merge state are separate from `journal.routing` (`lib/consumer-fanout.js:794–876,1352–1485`). Pending receipts are created lazily, not by every routing record operation (`:716,850–852`). |

### C4. Profile sidecar and projection

- **VERIFIED:** normalization accepts `_routing.mode: shadow`; agent entries may have `default`, `tier_from`, `route`, while gate entries use a separate closed validator (`lib/pipeline-profiles.js:40–60,67–130`). `route` is a closed object containing boolean `learn` (`:234–236`). Gate records do not accept route policy.
- **VERIFIED:** runtime merge refuses overriding `route` while preserving existing static semantics (`lib/pipeline-profiles.js:133–146`). Consumer tier selection still gives valid `item.tier` priority when `tier_from` is configured (`:148–156`). Route-only annotation does not itself enable legacy wave handling (`lib/build.js:797–800`).
- **VERIFIED:** `routingProfileProjection` validates routing mode/scope, removes `_routing` and per-agent `route`, converts a route-only wrapper to its legacy profile string, and preserves `{ default, tier_from }` when applicable (`lib/pipeline-profiles.js:241–262`). Other ordinary object shapes retain legacy representation. `preflightPipelineProfiles` computes digest from projected normalized/resolved/overrides and attaches hidden routing policy/static provenance (`:185–231`, digest `:226`, hidden properties `:227–230`). Build's wrapper also checks spec/dispatch compatibility (`lib/build.js:2106–2181`).

### C5. Contracts and absent homes

| Contract | VERIFIED content and limits |
|---|---|
| `contracts/routing-start.schema.json` | Draft-07 closed object, schema version 1, policy `s1a-static-v1`, shadow only (`:1–60`). Required identity/seed/policy/table/spec/input/profile/mapping/contract/static-resolution/calibration/cohort fields (`:8–34`). Policy fixes tolerance, empty trials, zero exploration; table is empty `s1a-empty-v1` with null cutoff (`:65–140`). Static calibration/cohort constants (`:248–255`). Closed Resolution and Provenance definitions include intended provider/template/tier/model/tools/thinking/effort/profile and static source/manual/item-tier provenance (`:259–448`). No model-call receipt joins or acceptance outcomes. |
| `contracts/routing-record.schema.json` | Closed `oneOf`: seven record types across ten branches — plan-intent (prepared/requested), run-binding, continuation-intent, epoch-binding, admission, issuance, issuance-event (`:6–43,88–122,158–197,355–395,517–563,665–708,787–1060`). All bind start/root/id/schema. Continuation graph/completion/index evidence (`:161–178`); epoch source graph and admitted/issued record references (`:358–376`); admission physical/logical identity, baseline/proposal/admitted/would/refusal/repairContext (`:520–544`); issuance key/token/predecessor/selected/would (`:668–689`). Event branches are launch-intent, result-prepared envelope, settled evidence, uncertain reason (`:790–821,840–889,894–1005,1009–1059`). |
| Settled evidence is not downstream outcome | Schema allows status succeeded/failed/cancelled (`contracts/routing-record.schema.json:995–1000`), but validator requires accepted token equal to issuance token (`lib/routing-ledger.js:480–484`). It has no D6 acceptance label, gate findings/dispositions, call list, cost completeness, receipt revision or ledger-version field. |
| Join schema — **NOT FOUND** | Contract file inventory contains routing-start and routing-record only; no `routing-join.schema.json`. The planned file is named in `docs/features/COMP-MODEL-ROUTE/design.md:480`; current schema loader compiles only start/record (`lib/routing-ledger.js:9–18`). Receipt `detail` is a transport escape hatch (`../stratum/ts/src/engine/receipts.ts:4–14`), not an existing validated routing join schema. |
| Outcome schema — **NOT FOUND** | No `routing-outcome.schema.json` in contract inventory. Planned at `docs/features/COMP-MODEL-ROUTE/design.md:481`; current closed record branches above have no outcome type. `repairContext` is not an implemented repair-floor/outcome schema: admission stores `{ state: 'not-evaluated-s1a' }` (`lib/build.js:1060`), and S1a validator enforces static admission invariants (`lib/routing-ledger.js:42–45`). No current latest-validated-version-per-id reader exists in this module (`lib/routing-ledger.js:576–592`). |

## Part D — ranked defect hazards and existing contradictions

Ranking is **INFER** based on the inspected reachability and number of affected paths, not a measured defect probability. Each entry distinguishes existing behavior from a potential S1b regression.

### 1. Missing original joins, invented paid identities and erased uncertainty

**VERIFIED current behavior:** no routing join reaches a receipt (`lib/build.js:2261–2285`); non-ceiling receipt failures are caught and warned (`:2291–2294`). Usage normalization and receipt conversion contain the following fallback sites. These are incompatible with treating the resulting row as automatically complete S1b evidence (`docs/features/COMP-MODEL-ROUTE/design.md:239–264`).

| Site | Identity / zero / provenance behavior |
|---|---|
| `usageRecordFromRaw`, `lib/result-normalizer.js:288–321` | Missing call id becomes `randomUUID` at `:298`; model/effort can use configuration; duration/split members default. All-zero or absent usage can return null via `:278–285`, losing an observation even if a call occurred. |
| Primary event usage, `lib/result-normalizer.js:480–496,716–720` | Event totals start at zero; missing explicit cost can be calculated/marked estimated; missing primary id is replaced by UUID; model/effort can fall back to configured values. Those values do not certify actual execution identity/tier. |
| Receipt construction, `lib/build.js:2262–2285` | Missing id becomes UUID; missing model becomes `unknown`, duration zero; one-sided split zero-fills the other side. `costUnknown` appears only under ceiling configuration. |
| Consumer and ordinary no-usage ceiling paths, `lib/build.js:1867–1868,5224` | Missing primary model-call id falls back to engine `dispatchToken`. This substitutes a different identity domain, not evidence of a paid call id. |
| `toEngineUsage` / `failureUsageFields`, `lib/build.js:2195–2227` | Totals default missing components to zero; output includes only positive totals. Explicit zero USD is omitted. Error aggregates can merge multiple call usages and choose later model fields; an aggregate does not preserve per-call attribution. |
| `runAgentText`, `lib/stratum-mcp-client.js:855–889` | Usage normalization can mint a fallback UUID and catches usage sink errors; returning text omits the wrapper's real id. |
| Local connector, `lib/local-claude-connector.js:197–213,253–300` | Totals begin zero; absent failure fields can be represented as zero; initial model is configured before observation. Actual wrapper id is minted before SDK invocation and does not prove a paid dispatch happened. |
| Stratum receipt normalization, `../stratum/ts/src/engine/receipts.ts:26–60` | Requires id but defaults telemetry model/duration. It cannot distinguish a fabricated caller id from a real connector id. |
| Engine legacy receipts, `../stratum/ts/src/engine/engine.ts:2743–2767` | Creates `legacy:<seq>` from step-done usage, not a provider call id. These are a different provenance class, as are zero-usage `compose:*` metadata (`lib/build.js:821–827`) and `engine:step_reset` (`../stratum/ts/src/engine/engine.ts:2805–2818`). |
| Legitimate non-call identities | Missing ordinary epoch maps to zero (`lib/flow-state.js:126–132`; `../stratum/ts/src/engine/state.ts:176`); missing task id can derive a logical allocation id (`lib/build.js:1275`); normalizer correlation id can use UUID (`lib/result-normalizer.js:401`). These are not paid-call/accepted-token evidence and cannot substitute for it. |

**INFER:** counting normalized receipt rows or positive aggregate usage as complete samples would silently conflate missing values, provider observation, engine issuance identity and metadata. `readFlowSpend` does not close that gap: no routing-join check and no-USD/no-positive-token-or-time rows are skipped (`lib/flow-state.js:63–71`).

### 2. No-journal artifact adapters are a real compatibility population

**VERIFIED:** all ten literal adapters below lack `journal` and drive the real shared `runConsumerIssuance` path. Some `prepareIssuance` methods return only a diff; three return `undefined`. The existing safe routing probe is `artifacts?.journal?.routing` (`lib/build.js:734`); prepared-envelope handling tolerates no returned envelope (`:1915–1921`). These are the touch points for any added outcome/join handling, even with routing off.

| Caller/helper | Artifact stub | Call site / shape |
|---|---|---|
| `driveConsumerItem` | `test/build-emission.test.js:258–264` | `:278–281`; returns `{ diff: null }`. |
| `driveItem` | `test/gsd-dispatch-instrumentation.test.js:64–70` | `:82–85`; returns `{ diff: diffText }`. |
| Runtime ownership/review test | `test/review-fixes-runtime.test.js:137` | `:136–137`; returns `{ diff: '' }`. |
| `driveConsumerIssuance` | `test/ts-cutover-e3-round3.test.js:59–65` | `:77–81`; captures envelope, returns nothing. |
| `driveConsumerIssuance` | `test/ts-cutover-e3-round4.test.js:52–58` | `:70–72`; captures envelope, returns nothing. |
| Late timeout test | `test/ts-cutover-e3-round5.test.js:216–222` | `:238–243`; captures envelope, returns nothing. |
| Consumer receipt test | `test/usage-receipts.test.js:699–704` | `:707–714`; returns `{ diff: '', envelope }`. |
| Consumer failed receipt test | `test/usage-receipts.test.js:768–775` | `:780–785`; returns `{ diff: '', envelope }`. |
| Cancellation/usage consumer test | `test/usage-receipts.test.js:1085–1087` | `:1091–1096`; returns `{ diff: '', envelope }`. |
| Lane pipeline `driveItem` | `test/integration/agent-lanes-pipeline.test.js:64–70` | `:82–85`; returns `{ diff: null }`. |

**VERIFIED additional no-artifact contexts:** direct receipt callers supply only flow/receipts/client, without `artifacts`: `test/usage-receipts.test.js:322,657,705,785,1096`; `test/review-fixes-runtime.test.js:65`; `test/round3-execution.test.js:40,70`; cancellation receipt calls at `test/build-cancel-detect.test.js:113–114`. Shared normalizer callers outside Build/GSD also have no routing requirement (`lib/new.js:139,159`; `lib/import.js:217`; `lib/step-validator.js:42`).

**VERIFIED existing sharp edge:** `flushWaveReceipts` uses `context.artifacts?.journal.pendingUsageReceipts`, which only guards the artifact object, not a missing journal (`lib/build.js:805`); checkpoint replication similarly reads `artifacts?.journal.wave` (`:1435–1447`). **INFER:** extending receipt/reconciliation calls onto these legacy adapters without respecting participation can recreate the prior full-suite failure population. The recorded 19 failures/three missed reviews are historical evidence in `docs/features/COMP-MODEL-ROUTE/progress.md:201–210`, not test results from this audit.

### 3. Existing S1a failed-issuance reconciliation does not match engine failure tokens

**VERIFIED:** routing settlement requires `acceptedDispatchToken === issuanceToken` (`lib/routing-ledger.js:480–484`), and reconciliation reads current accepted token/epoch/generation (`lib/build.js:1160–1165`). Consumer failure either retries with a new live token or exhausts without an accepted token (`../stratum/ts/src/engine/engine.ts:2009–2010,2051–2056`); ordinary `failAttempt` deletes both live and accepted tokens (`:2478–2503`). Retry issuance insertion demands a settled prior record (`lib/consumer-fanout.js:753–775`). The schema's allowance of `status: failed/cancelled` does not make those states reachable with valid evidence (`contracts/routing-record.schema.json:995–1000`).

**INFER — existing defect candidate:** an acknowledged failed `stepDone` can leave the routed issuance uncertain and prevent same-epoch retry admission. Failure/cancellation observation cannot be equated with the existing successful-token settlement test. This consequence was not exercised; source paths are contradictory enough to require explicit blueprint awareness.

### 4. Gate RPC resets before return; current index and artifact acceptance are insufficient

**VERIFIED:** normal revise clears/replaces carry and deletes old fanout/output/tokens inside the gate RPC (`../stratum/ts/src/engine/engine.ts:1345–1370,2823–2838`). Post-RPC “accepted” metadata in Build/GSD therefore occurs after possible destruction (`lib/build.js:5821,5906`; `lib/gsd.js:784`). Current artifact reconciliation supersedes missing/different-generation entries (`lib/consumer-fanout.js:1073–1100`). Worktree checkpoints omit isolation-none accepted tokens (`:1375,1470–1485`).

**INFER:** sampling only after gate acknowledgement or using the new item at old index loses retained/repaired distinctions; treating artifact acceptance/checkpoint membership as D6 acceptance mislabels isolation-none and retained previous-wave tasks. The design's `[A,B] → [B]` example explicitly disallows index identity and final-epoch blanket acceptance (`docs/features/COMP-MODEL-ROUTE/design.md:290–303`).

### 5. Repair call visibility differs from credited output; hook errors can be swallowed

**VERIFIED:** local primary repair uses MCP; both repair success and failure can add usage (`lib/result-normalizer.js:777–815`), but only credited repair id is returned in `dispatchIds` (`:846–851`). Normalization fallback swallows ordinary repair errors (`lib/review-normalize.js:153–164`). Build fixer, policy revision and review-gate fix are additional normalizer invocations (`lib/build.js:4782,4971,5736`), while GSD ordinary calls bypass it (`lib/gsd.js:651`).

**INFER:** an observer attached only to successful normalized return, only `dispatchIds.repair`, or only the primary MCP branch misses actual billed calls. An error thrown inside the existing repair try/catch can be interpreted as repair failure and swallowed by fallback unless its semantics are distinguishable. Existing aggregate repair folding adds only tokens/USD, not complete per-call time/cache evidence (`lib/result-normalizer.js:769–773`).

### 6. Off-mode byte identity spans much more than the router

**VERIFIED oracle:** the immutable fixtures are `test/fixtures/model-route-off-bundled-build-v0.5.1.json`, `test/fixtures/model-route-off-carry-v0.5.1.json`, and `test/fixtures/model-route-off-gsd-input-v0.5.1.json`. Fixture loader requires source revision `5fbf8e0bd5dae18eb92a08197b5a9a50743722dc` (`test/helpers/build-wave-golden-fixture.js:223–227`). The carry test compares digest/input/plan options/full normalized calls, absence of routing subtree/storage and unchanged git exclude (`test/integration/build-wave-golden.test.js:268–281`). Shadow comparison also pins full calls (`:285–300`). GSD frozen fixture covers plan input/options, not a full GSD call trace (`test/fixtures/model-route-off-gsd-input-v0.5.1.json:2–9,12–40`; historical scope explanation `docs/features/COMP-MODEL-ROUTE/reports/slice1a-d3-impl.md:69–90`). **The fixtures were not regenerated or modified.**

All following candidate S1b observation seams are also on existing off-mode paths; routing participation is not implied by reaching them:

| Off-mode surface | Evidence |
|---|---|
| Shared normalizer options, prompts, connector request, returned/error shape, event stream | `lib/result-normalizer.js:350–401,563–638,774–889`; `lib/stratum-mcp-client.js:182–196,268–332`; `lib/local-claude-connector.js:126–217`. Adding model-facing options or changing failure handling changes pre-routing behavior. |
| Primary/fixer/revision/gate-fixer/Q&A/escalation/probe launches | `lib/build.js:1766,3001,4172,4782,4879,4971,5736`; `lib/bug-escalation.js:125,321`; `lib/gsd.js:651`. These are not all reached through one routing hook. |
| Usage conversion, aggregation, receipt payload and delivery | `lib/build.js:1864–1875,2195–2298,4290–4316,5006,5224–5233`; `lib/gsd.js:673–685,1565–1583`; `lib/stratum-mcp-client.js:855–889`. New receipt fields, dispatch ids, RPCs or stricter failure propagation would be externally observable. |
| Consumer artifact preparation/replay and `stepDone` | `lib/build.js:723–794,1580–1645,1915–1948`; `lib/consumer-fanout.js:1198–1280`. Legacy adapters and returned envelope identity belong to this path. |
| Wave evidence, configured/output/cost/human/review gates, checkpoints, ship replay | `lib/build.js:803–827,1422–1508,5486–5906,6642–6653`; `lib/gsd.js:720–784`. New snapshots/metadata, ordering or gate outcomes can affect frozen receipt/call history. |
| Terminal/error/cancel/restart handling | `lib/build.js:3919–3952,3998–4025,5957–6030,6352–6445`; `lib/build-cancel.js:168`; `lib/gsd.js:348–478`. Introducing routing storage on these paths would violate no-routing-write evidence. |
| Profile projection / digest / plan transport / journal opening | `lib/pipeline-profiles.js:185–262`; `lib/routing-ledger.js:187–192`; `lib/consumer-fanout.js:410–436,533–559`. Off creation returns null; no-op journal mutations preserve bytes. The off reopen test verifies same journal bytes and refusal to adopt a routing root (`test/routing-journal.test.js:90–92`). |

**INFER:** passing tests against regenerated fixtures would not verify byte identity; fixture mutation would erase the historical oracle. Existing fixture sensitivity explicitly rejects added options, changed prompts and cross-step/wave reorderings (`test/integration/build-wave-golden.test.js:314–328`).

### 7. Concurrent call arrival order, concurrent journals and revision identity

**VERIFIED:** Build runs consumers concurrently and drains their completed queue (`lib/build.js:4510–4598`); S1a filters already-scheduled tokens for participating single-stage routing (`:4528`). The golden normalizer sorts only contiguous `execute/N` arrivals within one wave (`test/helpers/build-wave-golden-fixture.js:234–263`, especially `:239–244`). It does not imply deterministic arrival across runs or permit sorting all calls globally.

**VERIFIED:** receipt flush retains a pending list across awaited RPCs (`lib/build.js:803–819`); journal mutation reloads whole JSON with only an in-process writer guard (`lib/consumer-fanout.js:85–89,410–436`). Routing storage lock scope is one start (`lib/routing-ledger.js:132–150`). A project-wide JSONL writer/latest-version reader does not exist (`lib/routing-ledger.js:1–18,576–592`).

**INFER:** binding by ordinal of connector arrival can join the wrong item; concurrent flush/recovery may redeliver the same row; separate starts can contend on a shared project ledger. Existing immutable routing ids reject changed bytes under the same id (`lib/consumer-fanout.js:717–724`), whereas D6 outcome revisions intentionally reuse the logical record id (`docs/features/COMP-MODEL-ROUTE/design.md:305–307`). These are distinct idempotency contracts.

### 8. Late receipt redelivery and cancellation have different guarantees at each layer

**VERIFIED:** Compose spool refuses changed payload for an existing id (`lib/consumer-fanout.js:850–863`); Stratum returns already-recorded on matching id without comparing payload (`../stratum/ts/src/engine/engine.ts:890–892`). Cancelled runs reject before dedup (`:873–877`). Compose acknowledgement accepts optional sequence (`lib/consumer-fanout.js:866–872`), and usage reporting can catch spool/flush errors and only warn (`lib/build.js:2287–2294`). Continuation exports only routing ancestry, not the receipt spool (`lib/consumer-fanout.js:741–745`; `lib/routing-ledger.js:325–340`).

**INFER:** “already recorded” is not independently proof of matching original payload, and attempted redelivery after cancellation is not guaranteed to acknowledge. Terminal/continuation completion cannot convert unresolved intent into zero-cost execution. Current fail-open reporting also means a spool integrity refusal may be visible only as warning to its caller.

### 9. Validated gate findings do not yet establish a complete disposition partition

**VERIFIED:** assess prompt demands every original finding appear exactly once in addressed/open arrays (`presets/team-fable-astra.stratum.yaml:203–204`). `validateDecision` checks shapes/open_count/blocking/repair ownership but never compares the union and multiplicity against the original `review.findings` (`lib/output-gate.js:12–39`). `validateWaveAdmission` checks files/dependencies/tiers, not duplicate task ids or prior task identity (`lib/pipeline-profiles.js:157–177`); participating route admission separately refuses duplicate logical ids (`lib/build.js:1287`).

**INFER — existing validation gap:** a decision can satisfy these structural checks without providing a trustworthy exhaustive finding disposition. “Validated gate output” currently means these narrower checks; it does not prove all old tasks/findings were accepted, repaired or retained. Prompt-only no-lower-tier instruction has the same limitation (`presets/team-fable-astra.stratum.yaml:211–213`; `lib/output-gate.js:28–39`). S1b records history; enforcement remains S3 (`docs/features/COMP-MODEL-ROUTE/design.md:311–313`).

### 10. Execution tier can be falsely inferred from intended profile

**VERIFIED:** start mappings/static selected route are pinned (`lib/routing-ledger.js:248–258`; `lib/build.js:1092–1124`), but normalizer usage can fill model/effort from config (`lib/result-normalizer.js:298–321,716–720`). Local returned telemetry omits effort (`lib/local-claude-connector.js:287–300`); separate telemetry's effort is configured `appliedEffort` (`:68–90,151`). Current observed item-model receipt explicitly denies verified connector identity (`lib/build.js:1925–1927`).

**INFER:** selected tier/model or a normalized fallback cannot certify the actual executed tier needed for future repair-floor evidence. Missing/contradictory model/effort remains missing/contradictory, as D6 requires (`docs/features/COMP-MODEL-ROUTE/design.md:273–274`).

### 11. GSD direct receipt conversion drops available provenance/split

**VERIFIED — existing inconsistency:** GSD direct success handling reads `runOut.usage.usd_source`, but does not copy sibling `runOut.usdSource` or `runOut.split` (`lib/gsd.js:673–683`). The normalizer repair adapter explicitly consumes sibling `usdSource`/`split` (`lib/result-normalizer.js:788–794`), and `runAgentText` supports sibling provenance (`lib/stratum-mcp-client.js:874`). Receipt conversion strips USD lacking accepted provenance (`lib/build.js:2254–2257`). Direct GSD error handling passes `error.usage` and dispatch id without telemetry/split/provenance adaptation (`lib/gsd.js:663–668`).

**INFER:** a real returned dollar/split observation can become incomplete or disappear on the GSD ordinary path even when equivalent normalizer paths preserve it. JSON parsing after the direct call also occurs outside its call catch (`lib/gsd.js:661–670,691`), leaving a distinct post-call/pre-result failure seam.

### 12. GSD cancelled status and Build terminal labels are not an acceptance oracle

**VERIFIED — existing loop inconsistency:** GSD's status loop excludes completed/failed/stuck/waiting_gate/budget_exhausted, but not cancelled (`lib/gsd.js:348–355`); `runOneStep` falls through returning the unchanged response for other statuses (`:791`). **INFER:** a cancelled response reaching that loop can repeatedly return unchanged instead of reaching terminal accounting. No runtime reproduction was attempted.

**VERIFIED:** Build flow-completed handling can later downgrade application completion after health checks (`lib/build.js:5984–5997,6128`), while cancellation teardown owns separate terminal state (`:6352–6358`). GSD has orderly pause and continuation, not only terminal success/failure (`lib/gsd.js:358–394,220–240`). **INFER:** application terminal status cannot replace the per-issuance downstream evidence population defined by D6 (`docs/features/COMP-MODEL-ROUTE/design.md:281–303`).

### 13. Existing gate acknowledgement proof is ordinal, not token-bound

**VERIFIED:** both driver recovery catches use gate event ordinal/count (`lib/build.js:5601–5602`; `lib/gsd.js:767–768`), and restart artifact recovery uses stored gate ordinal or inferred offsets (`lib/consumer-fanout.js:1926–1937`). Engine `gate_resolved` records decision/target but omits the consumed gate token (`../stratum/ts/src/engine/engine.ts:1338,1350`). The persisted transaction does have a gate token (`lib/consumer-fanout.js:1352–1485`).

**INFER:** ordinal correlation has stricter completeness/order assumptions than exact gate-token reconciliation. It proves neither a complete pre-reset snapshot nor exact token identity by itself, especially for gates without a wave merge transaction. Current Build recovery is further limited by `waveProfilesEnabled` (`lib/build.js:5599–5602`).

### 14. Naming and comments can mislead an otherwise accurate census

- **VERIFIED:** `lib/routing-ledger.js` is currently start/record durability and validation, not the D6 JSONL outcome ledger (`:1–18,115–156`). `pendingRoutingPlans` includes bound plans (`:576–588`). `issuance-event: settled` is token-based engine settlement, not D6 final acceptance (`:457–488`).
- **VERIFIED:** consumer comment says Build passes no usage sink (`lib/build.js:1865–1866`), but Build installs one (`:4376`). The comment is stale; the execution path is not a no-op.
- **VERIFIED:** normalization `repairUsed` controls credited output identity, not whether a paid repair happened (`lib/result-normalizer.js:777–815,846–851`). Engine/Compose metadata dispatch ids and logical allocation ids are named similarly to paid ids but generated by different mechanisms (`lib/build.js:821–827,1275`; `../stratum/ts/src/engine/engine.ts:2754`).

These distinctions are material to interpreting the existing surfaces; none of the findings above modifies S1a code or supplies an S1b implementation design.
