# COMP-ROADMAP-ARCHIVE-LINKS: Incoming-link rewriting across registered consumer roots after archival. COMP-ROADMAP-ARCHIVE v1 rewrites links inside the two roadmap documents and leaves redirect anchors for everyone else. Extend relocation to explicitly registered consumer roots (feature folders, docs, journal) using the relocation map, confined to configured roots, excluding generated and vendor trees, never sweeping other repositories or external issue bodies. Features S low

**Status:** PLANNED
**Created:** 2026-09-09

---

## Intent

Incoming-link rewriting across registered consumer roots after archival. COMP-ROADMAP-ARCHIVE v1 rewrites links inside the two roadmap documents and leaves redirect anchors for everyone else. Extend relocation to explicitly registered consumer roots (feature folders, docs, journal) using the relocation map, confined to configured roots, excluding generated and vendor trees, never sweeping other repositories or external issue bodies. Features S low

---

## Notes

_This is a seed design doc created by `compose feature`. The `compose build` pipeline will expand it into a full design, blueprint, and implementation plan._
