{"attempt":1,"ts":"2026-09-19T09:02:42.737Z","hypothesis":"For fanout.dispatch:'engine' steps, per-item receipts are settled by the real @smartmemory/stratum engine (installed 0.6.0) via its external usageReport() RPC, which explicitly rejects 'legacy:'/'engine:' prefixed dispatchIds from external callers ('dispatchId prefix \"legacy:\" is reserved for engine-synthesized receipts', node_modules/@smartmemory/stratum/dist/engine/engine.js:632-637). Receipts settled this way therefore carry the real connector-invocation UUID as dispatchId, not 'legacy:<seq>'. This is already true in the engine's persisted flow-state snapshot before Compose's recovery code (lib/routing-runtime.js recoverRoutingEvidence -> lib/consumer-fanout.js recordRoutingEngineReceipt/recordRoutingObservation -> lib/routing-ledger.js prepareEngineReceiptEvidence) ever runs; that code copies the receipt verbatim and never rewrites dispatchId, and lib/routing-ledger.js:819's validateEngineObservation only conditionally checks the legacy: pattern (it does not require it). So the compose evidence-recovery machinery is behaving correctly and faithfully surfacing what the engine reported; test/build-model-route-outcomes.test.js:303's hardcoded 'dispatchId.startsWith(\"legacy:\")' assertion is the piece that no longer matches the current stratum engine's actual behavior for real dispatch:'engine' fanout receipts.","verdict":"accepted","evidence_for":[{"command":"Diagnostic script driving the exact fixture from test/build-model-route-outcomes.test.js's 'real engine-driven fanout' test, calling readRoutingSnapshot(f.flowId, {}) from lib/flow-state.js immediately after waitForReceipt(status==='completed') and BEFORE calling f.run({resumeFlowId}) (i.e. before any compose recovery code runs), then JSON.stringify(rawSnap.receipts)","actual_output":"Receipts array contains, for stepId 'engine_work' / source 'fanout': {\"seq\":3,\"dispatchId\":\"7c95f560-7134-48d5-9398-f8e99b5a0ade\",...,\"usdSource\":\"reported\"} and {\"seq\":4,\"dispatchId\":\"285b3b27-4915-4e29-ad46-87ebd255eaf3\",...,\"usdSource\":\"reported\"} — neither dispatchId starts with 'legacy:'","conclusion":"The non-legacy dispatchId is present in the engine's own persisted flow-state snapshot before Compose's recovery/observation code touches it, so the defect originates at the engine/runner settlement layer, not in Compose's recovery logic."},{"command":"grep -n \"dispatchId\" node_modules/@smartmemory/stratum/dist/engine/engine.js","actual_output":"Line 632-637: `if (candidate.dispatchId.startsWith(\"legacy:\")) { throw new ReceiptValidationError('dispatchId prefix \"legacy:\" is reserved for engine-synthesized receipts'); } if (candidate.dispatchId.startsWith(\"engine:\")) { throw ... 'engine:' is reserved ... }` inside the usageReport() RPC handler (line 611). Line 2539 is the ONLY site that literally builds `dispatchId: \\`legacy:${seq}\\`` and it lives inside settleLegacyReceipt, used for step_done/fanout/judged internal settlement.","conclusion":"The engine's own external-facing usageReport() contract forbids externally-reported receipts from using the 'legacy:' prefix — that prefix is reserved for the engine's internal synthesis path. A receipt with a real per-call UUID dispatchId is therefore evidence of a genuine (non-legacy) connector-invocation settlement, which is a legitimate, distinct receipt shape the test's blanket 'startsWith(legacy:)' assertion does not account for."},{"command":"env COMPOSE_PORT=19997 node --test --test-name-pattern=\"real engine-driven fanout\" test/build-model-route-outcomes.test.js  (captured to file, then grepped for AssertionError/actual/expected)","actual_output":"line 24: assert.ok(receipts.every(r => r.engineReceiptEvidence.receipt.dispatchId.startsWith('legacy:'))) / name: 'AssertionError' / expected: true / actual: false","conclusion":"Confirms the live test run reproduces the exact assertion failure, consistent with the raw snapshot observation above — the same non-legacy dispatchId is what the test's own assertion evaluates to false against."}]}
